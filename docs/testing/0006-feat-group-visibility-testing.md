# Тестирование: Group Visibility Settings (0006)

**Спецификация:** [docs/specs/0006-feat-group-visibility.md](../specs/0006-feat-group-visibility.md)
**Дата:** 2026-02-20

---

## 1. Подготовка

- [ ] Собрать плагин: `npm run build`
- [ ] Установить в тестовый Obsidian vault
- [ ] Открыть Developer Tools (Ctrl+Shift+I) — следить за ошибками в консоли

---

## 2. Happy Path

### Сценарий 1: Скрыть группу и восстановить

**Предусловие:** доска с задачами в `orgIntentions` (создать 2 задачи)

**Шаги:**
1. [ ] Нажать ⚙️ (настройки доски) → попап открылся
2. [ ] В секции "Видимость групп" найти строку `Org Intentions (2)` → счётчик показан
3. [ ] Снять галочку у `Org Intentions` → галочка снята, строка не задизейблена
4. [ ] Нажать **Save** → попап закрылся
5. [ ] Убедиться: группа `Org Intentions` исчезла с доски
6. [ ] Убедиться: остальные группы на месте, без визуальных артефактов
7. [ ] Открыть настройки доски снова → `Org Intentions` без галочки
8. [ ] Поставить галочку → нажать **Save**
9. [ ] Убедиться: группа появилась обратно с теми же 2 задачами

### Сценарий 2: Счётчик задач

**Шаги:**
1. [ ] Открыть настройки доски
2. [ ] Группа с задачами → рядом с названием отображается `(N)`, где N > 0
3. [ ] Группа без задач → счётчик не отображается (только название)
4. [ ] Проверить несколько групп с разным количеством задач

### Сценарий 3: Cancel сбрасывает изменения

**Шаги:**
1. [ ] Открыть настройки доски
2. [ ] Снять галочку с нескольких групп (не нажимать Save)
3. [ ] Нажать **Cancel**
4. [ ] Убедиться: все группы на доске на месте (изменения не применились)
5. [ ] Открыть настройки снова → галочки вернулись в исходное состояние

### Сценарий 4: Клик по overlay сбрасывает изменения

**Шаги:**
1. [ ] Открыть настройки доски
2. [ ] Снять галочку с одной группы
3. [ ] Кликнуть вне попапа (по затемнённому overlay)
4. [ ] Убедиться: попап закрылся, доска не изменилась
5. [ ] Открыть настройки снова → галочка на месте

### Сценарий 5: Настройки per-board

**Шаги:**
1. [ ] Создать вторую доску (+)
2. [ ] На первой доске скрыть `delegated`
3. [ ] Перейти на вторую доску → `delegated` видна
4. [ ] Скрыть `focus` на второй доске
5. [ ] Перейти обратно на первую → `focus` видна, `delegated` скрыта
6. [ ] Перезагрузить Obsidian → настройки каждой доски сохранились

---

## 3. Автоматизации при скрытых группах

### Сценарий 6: Завершить задачу при скрытой `completed`

**Предусловие:** скрыть группу `completed`

**Шаги:**
1. [ ] Нажать иконку ✓ на карточке задачи в любой видимой группе
2. [ ] Задача исчезла с видимой части доски — без ошибок в консоли
3. [ ] Тост "Завершена: ..." появился
4. [ ] Нажать **Отменить** в тосте → задача вернулась в исходную группу
5. [ ] Показать группу `completed` → завершённая задача там

### Сценарий 7: Удалить задачу при скрытой `completed`

**Предусловие:** скрыть группу `completed`

**Шаги:**
1. [ ] Нажать иконку 🗑 на карточке задачи
2. [ ] Тост "Удалено: ..." появился, задача исчезла
3. [ ] Нажать **Отменить** → задача вернулась
4. [ ] Дождаться истечения 7 сек → задача удалена окончательно, ошибок нет

### Сценарий 8: Add task при скрытом `backlog`

**Шаги:**
1. [ ] Скрыть группу `backlog`
2. [ ] Убедиться: кнопка «Добавить задачу» есть в любой другой видимой группе
3. [ ] Добавить задачу через видимую группу → задача создана корректно

---

## 4. Защита от скрытия последней группы

### Сценарий 9: Блокировка последнего свитчера

**Шаги:**
1. [ ] Открыть настройки доски
2. [ ] Поочерёдно снять галочки с 5 групп (осталась 1 видимая)
3. [ ] Убедиться: галочка у последней видимой группы задизейблена (`disabled` на `<input>`)
4. [ ] Навести курсор на строку с задизейбленной группой → появляется tooltip «At least one group must be visible» (EN) или «Хотя бы одна группа должна быть видима» (RU)
5. [ ] Попытаться кликнуть по задизейбленному чекбоксу → ничего не происходит
6. [ ] Поставить галочку у другой группы → задизейбленный чекбокс разблокировался

---

## 5. Layout при скрытии групп focus / inProgress

### Сценарий 10: Скрыть `focus` — `inProgress` занимает полную ширину

**Шаги:**
1. [ ] Скрыть группу `focus` → нажать Save
2. [ ] Убедиться: `inProgress` занимает всю ширину строки (не половину)
3. [ ] Открыть настройки, вернуть `focus` → обе группы снова в два столбца

### Сценарий 11: Скрыть `inProgress` — `focus` занимает полную ширину

**Шаги:**
1. [ ] Скрыть группу `inProgress` → нажать Save
2. [ ] Убедиться: `focus` занимает всю ширину строки
3. [ ] Открыть настройки, вернуть `inProgress` → обе группы снова в два столбца

### Сценарий 12: Скрыть оба (`focus` и `inProgress`)

**Шаги:**
1. [ ] Скрыть обе группы → нажать Save
2. [ ] Убедиться: строка с этими группами полностью исчезла, нет пустого пространства
3. [ ] Вернуть обе → строка появилась, два столбца

---

## 6. Drag & Drop

### Сценарий 13: DnD между видимыми группами

**Шаги:**
1. [ ] Скрыть `orgIntentions`
2. [ ] Перетащить задачу из `backlog` в `focus` → задача перемещена, статус обновился
3. [ ] Перетащить задачу из `focus` в `inProgress` → работает корректно
4. [ ] Убедиться: нет ошибок в консоли

---

## 7. Edge Cases

### Сценарий 14: Многократное переключение свитчеров до Save

**Шаги:**
1. [ ] Открыть настройки доски
2. [ ] Снять галочку у `backlog` → поставить обратно → снять → поставить (4 раза)
3. [ ] Нажать Save
4. [ ] Убедиться: `backlog` видим (галочка стоит — финальное состояние)
5. [ ] Проверить данные через DevTools: `hiddenGroups` не содержит дублей

### Сценарий 15: Скрыть группу с `collapsed: true`

**Предусловие:** группа `backlog` свёрнута (collapsed)

**Шаги:**
1. [ ] Скрыть `backlog` → Save
2. [ ] Показать `backlog` → Save
3. [ ] Убедиться: `backlog` отображается в своём `collapsed` состоянии (не сбросилось)

### Сценарий 16: Перетащить задачу в группу, затем скрыть её

**Шаги:**
1. [ ] Перетащить задачу в `delegated`
2. [ ] Открыть настройки, скрыть `delegated` → Save
3. [ ] Открыть настройки, показать `delegated` → Save
4. [ ] Убедиться: задача на месте

---

## 8. Миграция данных

### Сценарий 17: Миграция version=3 → 4

**Предусловие:** в `data.json` плагина установить `"version": 3` вручную, удалить поле `hiddenGroups` у всех досок

**Шаги:**
1. [ ] Перезагрузить Obsidian (или выключить/включить плагин)
2. [ ] Убедиться: все группы видимы (без изменений)
3. [ ] Убедиться: данные задач не изменились
4. [ ] Убедиться: `data.json` теперь содержит `"version": 4` и `"hiddenGroups": []` у каждой доски

---

## 9. Локализация

### Сценарий 18: Переключение языка

**Шаги:**
1. [ ] Установить язык: English → открыть настройки доски
2. [ ] Убедиться: заголовок секции «Group Visibility», описание «Show or hide groups on this board»
3. [ ] Tooltip у задизейбленного свитчера: «At least one group must be visible»
4. [ ] Переключить на Русский → открыть настройки доски
5. [ ] Убедиться: «Видимость групп», «Показывать или скрывать группы на этой доске»
6. [ ] Tooltip: «Хотя бы одна группа должна быть видима»

---

## 10. Визуальные проверки

### Сценарий 19: Light / Dark тема

**Шаги:**
1. [ ] Проверить секцию "Видимость групп" в попапе в **light** теме:
   - [ ] Заголовок секции (uppercase, muted)
   - [ ] Разделители между строками групп
   - [ ] Задизейбленный чекбокс (opacity 0.4)
   - [ ] Счётчик задач (muted color)
2. [ ] Переключить на **dark** тему → повторить проверки
3. [ ] Убедиться: нет жёстко заданных цветов, всё через CSS-переменные Obsidian

---

## Итог

**Статус:** ⬜ Не проверено
