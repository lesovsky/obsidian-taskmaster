# Быстрое закрытие задачи (Quick Complete)

---

# Часть 1: Продуктовая

## Что меняется для пользователя

- На каждой карточке задачи появляется **кнопка быстрого закрытия** (рядом с кнопкой удаления)
- Один клик по кнопке — задача автоматически переводится в статус `completed` и перемещается в начало группы «Завершённые»
- После закрытия появляется **toast с возможностью отмены** (7 секунд), аналогично удалению
- Кнопка присутствует в **обоих режимах** карточки — обычном и компактном
- Если задача уже находится в группе «Завершённые» — кнопка **видна, но неактивна** (disabled)

---

## Зачем это нужно

Текущий способ закрыть задачу требует трёх действий:

1. Кликнуть по карточке → открывается модалка
2. Найти поле «Статус» и поменять на «Завершена»
3. Нажать «Сохранить»

Либо через drag-and-drop: перетащить карточку в группу «Завершённые» — физически далеко, группа свёрнута по умолчанию.

**Quick Complete сокращает это до одного клика** прямо на карточке. Это критично для повседневного flow проектного менеджера, когда нужно быстро «закрыть» несколько задач по итогам встречи или рабочего дня, не отвлекаясь на детали.

---

## Внешний вид кнопки

Кнопка отображается иконкой `☑` серого цвета — визуально напоминает пустую галочку, сигнализирующую о возможности завершить задачу.

Обе кнопки (`☑` и `✕`) расположены в **правом верхнем углу** карточки, так как кнопка удаления в обычном режиме — `position: absolute; top: 0.25rem; right: 0.25rem`. Кнопка `☑` размещается левее: `top: 0.25rem; right: 1.75rem`.

**Обычный режим карточки:**

```
┌────────────────────────────────────────────────────────────┐
│ 🔴 ⏳  📅 2026-03-24                           [☑]  [✕]   │
│ Подготовить отчёт для совета директоров                    │
│ 👤 Иванов А.                                               │
└────────────────────────────────────────────────────────────┘
```

Кнопки позиционированы абсолютно поверх контента первой строки. Видны при наведении на карточку (`:hover`), скрыты в покое — как существующая кнопка `✕`.

**Компактный режим карточки:**

```
┌──────────────────────────────────────────────────────┐
│ 🔴⏳  Иванов А.  Подготовить отчёт...  📅 24  [☑] [✕] │
└──────────────────────────────────────────────────────┘
```

В компактном режиме кнопки в потоке flex-строки справа, всегда видны — как существующая кнопка `✕`.

---

## Поведение при нажатии

1. Задача **удаляется из текущей группы**
2. Задача **добавляется в начало** `taskIds` группы «Завершённые»
3. Статус задачи меняется на `completed`, устанавливается `completedAt = сегодня`
4. Появляется **toast-уведомление**: «Завершена: [название задачи]» с кнопкой «Отменить» и таймером 7 сек

Если «Завершённые» свёрнуты — группа **не разворачивается** (аналогично поведению drag-and-drop в свёрнутую группу).

---

## Отмена (Undo)

Toast работает по тем же правилам, что и при удалении:

- Таймер 7 секунд с обратным отсчётом
- Кнопка «Отменить» — задача возвращается в исходную группу на исходную позицию, статус и `completedAt` восстанавливаются
- Максимум 3 toast'а одновременно (единая очередь с удалением). 4-й toast вытесняет самый старый
- При вытеснении toast'а типа `complete` ничего дополнительно не делается (задача остаётся в «Завершённых»), в отличие от вытеснения toast'а типа `delete` (задача окончательно удаляется)
- При истечении таймера toast'а типа `complete` — ничего не происходит (задача уже в «Завершённых», данные не удаляются)

**Поведение при закрытии Obsidian во время активного toast:** задача уже сохранена в «Завершённых» через `persist()` в момент нажатия кнопки. Если Obsidian закрыть до истечения toast, задача останется в «Завершённых» — undo больше недоступен. Это отличается от удаления, где закрытие Obsidian во время toast тоже необратимо удаляет задачу.

---

## Обработка граничных случаев

### Случай 1: Задача уже в группе «Завершённые»

Кнопка отображается, но **неактивна** (`disabled`, `opacity: 0.3`, `cursor: default`). Tooltip при наведении меняется на «Задача уже завершена».

**Известное поведение:** кнопка имеет `pointer-events: none`, поэтому клик в её область «проваливается» сквозь неё к родительскому элементу карточки — открывается модалка редактирования. Это ожидаемое поведение: пользователь получает возможность посмотреть или отредактировать уже завершённую задачу. Повторного завершения не происходит.

```
┌─────────────────────────────────────────────┐   ← карточка в группе «Завершённые»
│ ✅ 🟡  📅 2026-01-10                [☑̶]  [✕] │   ← ☑ затемнён, неактивен
│ Написать тесты для API                      │
│ 👤 Петров Б.                                │
└─────────────────────────────────────────────┘
```

### Случай 2: Нажатие на кнопку во время активного toast'а для этой же задачи

Не происходит — после первого нажатия задача уже переместилась в «Завершённые», кнопка стала неактивной.

### Случай 3: Несколько задач закрыты быстро подряд

Toast'ы стакаются (максимум 3). При 4-м нажатии самый старый toast вытесняется без отмены — задача просто остаётся в «Завершённых».

### Случай 4: Задача из Бэклога (статус `new`)

Статус задачи становится `completed` (а не `inProgress` через промежуточный шаг). Quick Complete — это прямой переход в завершение, минуя статусные правила промежуточных групп.

### Случай 5: Задача со статусом `waiting`

Статус меняется на `completed`. Никаких дополнительных предупреждений не показывается.

---

## Когда кнопка видна

| Ситуация | Состояние кнопки |
|----------|-----------------|
| Задача в любой рабочей группе или бэклоге | Активна |
| Задача в группе «Завершённые» | Видна, но неактивна (disabled); клик открывает модалку редактирования |

---

# Часть 2: Техническая

## Изменения в структуре данных

### Изменения в data.json

Изменений в схеме данных **нет**. Поля `status` и `completedAt` в модели `Task` уже существуют. Миграция не требуется.

### TypeScript — `Toast`: discriminated union вместо optional полей

Текущий `Toast` в `src/stores/uiStore.ts`:

```typescript
interface Toast {
  taskId: string;
  groupId: string;
  boardId: string;
  position: number;
  timerId: ReturnType<typeof setTimeout>;
  expiresAt: number;
}
```

Заменяется на **discriminated union** двух отдельных типов. Это даёт полную type safety в TypeScript без `!`-assertions и позволяет компилятору автоматически сужать тип в `if/else` блоках:

```typescript
// src/stores/uiStore.ts
import type { GroupId, Status } from '../data/types';  // ← новый импорт

type DeleteToast = {
  type: 'delete';
  taskId: string;
  groupId: GroupId;
  boardId: string;
  position: number;
  timerId: ReturnType<typeof setTimeout>;
  expiresAt: number;
};

type CompleteToast = {
  type: 'complete';
  taskId: string;
  groupId: GroupId;
  boardId: string;
  position: number;
  timerId: ReturnType<typeof setTimeout>;
  expiresAt: number;
  previousStatus: Status;        // обязательное, не optional
  previousCompletedAt: string;   // обязательное, не optional
};

type Toast = DeleteToast | CompleteToast;

interface UiState {
  activeBoardId: string;
  toasts: Toast[];
}
```

**Почему не `interface` с optional полями:** вариант с `previousStatus?: Status` требовал бы `!`-assertions (`toast.previousStatus!`) при обращении к полям `CompleteToast` — это отключает TypeScript strict mode для этих значений. С discriminated union компилятор сам знает, что внутри `if (toast.type === 'complete')` поля `previousStatus` и `previousCompletedAt` гарантированно не undefined.

**Побочный эффект:** `toast.groupId as GroupId` cast в существующем `handleUndo()` тоже становится ненужным — `groupId` теперь типизирован как `GroupId`.

---

## Изменения в компонентах

### 1. `src/stores/dataStore.ts` — новые функции

**Новый импорт** (добавить `Status` к существующим):

```typescript
import type { PluginData, Task, GroupId, Board, Settings, Status } from '../data/types';
```

**`quickCompleteTask()`** — перемещает задачу в «Завершённые» и обновляет статус:

```typescript
export function quickCompleteTask(
  taskId: string,
  sourceGroupId: GroupId,
  boardId: string,
): { position: number; previousStatus: Status; previousCompletedAt: string } | null {
  let result: { position: number; previousStatus: Status; previousCompletedAt: string } | null = null;

  dataStore.update(data => {
    const board = data.boards.find(b => b.id === boardId);
    if (!board) return data;

    const sourceArr = board.groups[sourceGroupId].taskIds;
    const idx = sourceArr.indexOf(taskId);
    if (idx === -1) return data;

    const task = data.tasks[taskId];
    if (!task) return data;

    // Сохраняем состояние для undo
    result = {
      position: idx,
      previousStatus: task.status,
      previousCompletedAt: task.completedAt,  // string, может быть '' для новых задач
    };

    // Убираем из исходной группы
    sourceArr.splice(idx, 1);

    // Добавляем в начало «Завершённых»
    board.groups.completed.taskIds.unshift(taskId);

    // Обновляем задачу
    task.status = 'completed';
    task.completedAt = formatDate(new Date());

    return data;
  });

  persist();
  return result;
}
```

**Примечание:** паттерн с `let result = null` + мутация внутри `update()` callback идентичен существующей `removeTaskFromGroup()` — callback Svelte writable store выполняется синхронно, поэтому `result` гарантированно заполнен до `return result`.

**`undoQuickComplete()`** — откатывает быстрое закрытие:

```typescript
export function undoQuickComplete(
  taskId: string,
  sourceGroupId: GroupId,
  boardId: string,
  position: number,
  previousStatus: Status,
  previousCompletedAt: string,
): void {
  dataStore.update(data => {
    const board = data.boards.find(b => b.id === boardId);
    if (!board) return data;

    // Убираем из «Завершённых»
    const completedArr = board.groups.completed.taskIds;
    const completedIdx = completedArr.indexOf(taskId);
    if (completedIdx !== -1) completedArr.splice(completedIdx, 1);

    // Восстанавливаем в исходной группе на исходной позиции
    board.groups[sourceGroupId].taskIds.splice(position, 0, taskId);

    // Восстанавливаем статус задачи
    const task = data.tasks[taskId];
    if (task) {
      task.status = previousStatus;
      task.completedAt = previousCompletedAt;
    }

    return data;
  });

  persist();
}
```

---

### 2. `src/ui/TaskCard.svelte` — новый prop и кнопка

**Новые props:**

```svelte
export let onComplete: () => void;
export let isInCompletedGroup: boolean = false;
```

**Кнопка в обычном режиме** (добавить рядом с `.tm-task-card__delete`):

```svelte
<button
  class="tm-task-card__complete clickable-icon"
  class:tm-task-card__complete--done={isInCompletedGroup}
  on:click|stopPropagation={onComplete}
  disabled={isInCompletedGroup}
  title={isInCompletedGroup ? $t('taskCard.alreadyCompleted') : $t('taskCard.completeTask')}
>☑</button>
```

**Кнопка в компактном режиме** (добавить перед `.tm-task-card__delete-compact`):

```svelte
<button
  class="tm-task-card__complete-compact clickable-icon"
  class:tm-task-card__complete--done={isInCompletedGroup}
  on:click|stopPropagation={onComplete}
  disabled={isInCompletedGroup}
  title={isInCompletedGroup ? $t('taskCard.alreadyCompleted') : $t('taskCard.completeTask')}
>☑</button>
```

---

### 3. `src/ui/TaskGroup.svelte` — новый prop и проброс в TaskCard

**Новый prop:**

```svelte
export let onCardComplete: ((taskId: string) => void) | null = null;
```

**Проброс в TaskCard:**

```svelte
<TaskCard
  {task}
  onClick={() => onCardClick(task)}
  onDelete={() => onCardDelete(task.id)}
  onComplete={() => onCardComplete && onCardComplete(task.id)}
  isInCompletedGroup={groupId === 'completed'}
/>
```

---

### 4. `src/ui/CollapsibleGroup.svelte` — новый prop и проброс в TaskCard

**Новый prop:**

```svelte
export let onCardComplete: ((taskId: string) => void) | null = null;
```

**Проброс в TaskCard:**

```svelte
<TaskCard
  {task}
  onClick={() => onCardClick && onCardClick(task)}
  onDelete={() => onCardDelete && onCardDelete(task.id)}
  onComplete={() => onCardComplete && onCardComplete(task.id)}
  isInCompletedGroup={groupId === 'completed'}
/>
```

---

### 5. `src/ui/BoardLayout.svelte` — обработчики и подключение

**Новые импорты:**

```typescript
import { quickCompleteTask, undoQuickComplete } from '../stores/dataStore';
```

#### Вспомогательная функция `evictOldestToastIfNeeded()`

Логика вытеснения вынесена в отдельный хелпер, чтобы избежать дублирования и гарантировать корректную обработку обоих типов toast. Вызывается из `handleDelete()` и `handleComplete()`.

```typescript
function evictOldestToastIfNeeded(): void {
  uiStore.update(ui => {
    if (ui.toasts.length >= MAX_TOASTS) {
      const oldest = ui.toasts[0];
      clearTimeout(oldest.timerId);
      if (oldest.type === 'delete') {
        finalDeleteTask(oldest.taskId); // только для delete: окончательное удаление
      }
      // для complete: ничего не делаем, задача остаётся в «Завершённых»
      ui.toasts = ui.toasts.slice(1);
    }
    return ui;
  });
}
```

#### Обновлённая функция `handleDelete()`

⚠️ **Обязательно обновить** — иначе при вытеснении toast'а типа `complete` из очереди `handleDelete()` вызовет `finalDeleteTask()` на задаче, которая была лишь завершена, и **безвозвратно уничтожит её данные**.

```typescript
function handleDelete(taskId: string, groupId: GroupId) {
  const result = removeTaskFromGroup(taskId, groupId, board.id);
  if (!result) return;

  evictOldestToastIfNeeded();  // ← заменяет инлайн-логику

  const expiresAt = Date.now() + 7000;
  const timerId = setTimeout(() => {
    finalDeleteTask(taskId);
    uiStore.update(ui => ({
      ...ui,
      toasts: ui.toasts.filter(t => t.taskId !== taskId),
    }));
  }, 7000);

  uiStore.update(ui => ({
    ...ui,
    toasts: [
      ...ui.toasts,
      {
        type: 'delete',          // ← обязательно указывать тип
        taskId,
        groupId,
        boardId: board.id,
        position: result.position,
        timerId,
        expiresAt,
      },
    ],
  }));
}
```

#### Новая функция `handleComplete()`

```typescript
function handleComplete(taskId: string, sourceGroupId: GroupId) {
  if (sourceGroupId === 'completed') return; // защита на случай UI-бага

  const result = quickCompleteTask(taskId, sourceGroupId, board.id);
  if (!result) return;

  evictOldestToastIfNeeded();

  const expiresAt = Date.now() + 7000;
  const timerId = setTimeout(() => {
    // Для complete: при истечении ничего не делаем, задача в «Завершённых»
    uiStore.update(ui => ({
      ...ui,
      toasts: ui.toasts.filter(t => t.taskId !== taskId),
    }));
  }, 7000);

  uiStore.update(ui => ({
    ...ui,
    toasts: [
      ...ui.toasts,
      {
        type: 'complete',
        taskId,
        groupId: sourceGroupId,
        boardId: board.id,
        position: result.position,
        timerId,
        expiresAt,
        previousStatus: result.previousStatus,
        previousCompletedAt: result.previousCompletedAt,
      },
    ],
  }));
}
```

#### Обновлённая функция `handleUndo()`

С discriminated union TypeScript автоматически сужает тип — никаких `!`-assertions не нужно:

```typescript
function handleUndo(taskId: string) {
  const ui = get(uiStore);
  const toast = ui.toasts.find(t => t.taskId === taskId);
  if (!toast) return;

  clearTimeout(toast.timerId);

  if (toast.type === 'delete') {
    // TypeScript знает: это DeleteToast, groupId — GroupId
    restoreTaskToGroup(taskId, toast.groupId, toast.boardId, toast.position);
  } else {
    // TypeScript знает: это CompleteToast, previousStatus и previousCompletedAt — не undefined
    undoQuickComplete(
      taskId,
      toast.groupId,
      toast.boardId,
      toast.position,
      toast.previousStatus,
      toast.previousCompletedAt,
    );
  }

  uiStore.update(u => ({
    ...u,
    toasts: u.toasts.filter(t => t.taskId !== taskId),
  }));
}
```

#### Подключение `onCardComplete` во всех группах шаблона

```svelte
<!-- backlog (CollapsibleGroup) -->
<CollapsibleGroup
  groupId="backlog"
  ...
  onCardDelete={(taskId) => handleDelete(taskId, 'backlog')}
  onCardComplete={(taskId) => handleComplete(taskId, 'backlog')}
/>

<!-- focus (TaskGroup) -->
<TaskGroup
  groupId="focus"
  ...
  onCardDelete={(taskId) => handleDelete(taskId, 'focus')}
  onCardComplete={(taskId) => handleComplete(taskId, 'focus')}
/>

<!-- inProgress (TaskGroup) -->
<TaskGroup
  groupId="inProgress"
  ...
  onCardDelete={(taskId) => handleDelete(taskId, 'inProgress')}
  onCardComplete={(taskId) => handleComplete(taskId, 'inProgress')}
/>

<!-- orgIntentions (TaskGroup) -->
<TaskGroup
  groupId="orgIntentions"
  ...
  onCardDelete={(taskId) => handleDelete(taskId, 'orgIntentions')}
  onCardComplete={(taskId) => handleComplete(taskId, 'orgIntentions')}
/>

<!-- delegated (TaskGroup) -->
<TaskGroup
  groupId="delegated"
  ...
  onCardDelete={(taskId) => handleDelete(taskId, 'delegated')}
  onCardComplete={(taskId) => handleComplete(taskId, 'delegated')}
/>

<!-- completed (CollapsibleGroup) — onCardComplete передаётся, но handleComplete
     защищён проверкой sourceGroupId === 'completed', кнопка в TaskCard disabled -->
<CollapsibleGroup
  groupId="completed"
  ...
  onCardDelete={(taskId) => handleDelete(taskId, 'completed')}
  onCardComplete={(taskId) => handleComplete(taskId, 'completed')}
/>
```

---

### 6. `src/ui/DeleteToast.svelte` — обобщение текста

Текущий компонент жёстко использует перевод `toast.deleted`. Нужно сделать текст динамическим:

**Заменить prop `taskName: string` на `message: string`:**

```svelte
<script lang="ts">
  export let message: string;  // ← вместо taskName
  export let expiresAt: number;
  export let onUndo: () => void;
  export let onExpire: () => void;
  // ... остальное без изменений
</script>

<div class="tm-toast">
  <span class="tm-toast__text">{message}</span>
  <span class="tm-toast__timer">{remaining}{$t('toast.seconds')}</span>
  <button class="tm-toast__undo" on:click={onUndo}>{$t('toast.undo')}</button>
</div>
```

**В `BoardLayout.svelte`** — формировать `message` при рендере toasts:

```svelte
{#each $uiStore.toasts as toast (toast.taskId)}
  <DeleteToast
    message={toast.type === 'delete'
      ? `${$t('toast.deleted')} ${tasks[toast.taskId]?.what ?? $t('fallback.task')}`
      : `${$t('toast.completed')} ${tasks[toast.taskId]?.what ?? $t('fallback.task')}`}
    expiresAt={toast.expiresAt}
    onUndo={() => handleUndo(toast.taskId)}
    onExpire={() => handleToastExpire(toast.taskId)}
  />
{/each}
```

---

### 7. `src/i18n/en.ts`, `src/i18n/ru.ts` и `src/i18n/types.ts` — новые ключи

Новые ключи нужно добавить в **три файла**. Пропуск `types.ts` вызовет ошибку компиляции TypeScript, так как `TranslationKey` — это строгий union-тип.

**`src/i18n/types.ts` — добавить в `TranslationKey`:**

```typescript
| 'taskCard.completeTask'
| 'taskCard.alreadyCompleted'
| 'toast.completed'
```

**`src/i18n/en.ts` — добавить:**

```typescript
'taskCard.completeTask': 'Complete task',
'taskCard.alreadyCompleted': 'Task already completed',
'toast.completed': 'Completed:',
```

**`src/i18n/ru.ts` — добавить:**

```typescript
'taskCard.completeTask': 'Завершить задачу',
'taskCard.alreadyCompleted': 'Задача уже завершена',
'toast.completed': 'Завершена:',
```

---

### 8. `src/styles.css` — новые классы

```css
/* ─── Quick Complete Button — обычный режим ─── */

.tm-task-card__complete {
  position: absolute;
  top: 0.25rem;           /* совпадает с .tm-task-card__delete */
  right: 1.75rem;         /* 0.25rem (right delete) + 1.25rem (ширина delete) + 0.25rem (gap) */
  width: 1.25rem;
  height: 1.25rem;
  font-size: 0.85rem;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  border: none;
  background: transparent;
  color: var(--text-faint);
  cursor: pointer;
  opacity: 0;
  transition: opacity 0.15s ease;
}

.tm-task-card:hover .tm-task-card__complete {
  opacity: 1;
}

.tm-task-card__complete:hover {
  background: var(--background-modifier-hover);
  color: var(--interactive-success);  /* семантически верно; --color-green нестабилен в темах */
}

/* ─── Quick Complete Button — компактный режим ─── */

.tm-task-card__complete-compact {
  flex-shrink: 0;
  width: 1.25rem;
  height: 1.25rem;
  font-size: 0.85rem;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  border: none;
  background: transparent;
  color: var(--text-faint);
  cursor: pointer;
  opacity: 1; /* всегда видна в compact, как кнопка удаления */
}

.tm-task-card__complete-compact:hover {
  background: var(--background-modifier-hover);
  color: var(--interactive-success);
}

/* ─── Неактивное состояние (задача уже завершена) ─── */

.tm-task-card__complete--done,
.tm-task-card__complete--done:hover {
  opacity: 0.3;
  cursor: default;
  color: var(--text-faint);
  background: transparent;
  pointer-events: none;
}
```

**Примечание о `--interactive-success`:** используется вместо `--color-green`, так как `--color-green` относится к нестабильной внутренней палитре Obsidian и может отсутствовать в темах сообщества. `--interactive-success` семантически точнее описывает действие «завершить задачу» и поддерживается шире.

---

## Миграция данных

Изменений в схеме `data.json` нет. Поля `status` и `completedAt` уже существуют в модели `Task`. Миграция не требуется.

---

## Тестирование

### Функциональные сценарии

1. **Базовое закрытие задачи**
   - Создать задачу в любой рабочей группе
   - Навести на карточку → нажать `☑`
   - Проверить: задача появилась в начале «Завершённых», статус `completed`, иконка ✅
   - Проверить: в исходной группе задачи нет

2. **Закрытие через компактный режим**
   - Переключить в Settings → Card view: Compact
   - Нажать `☑` на компактной карточке
   - Проверить те же условия, что в п.1

3. **Toast и undo**
   - Нажать `☑` → проверить появление toast «Завершена: [название]» с таймером
   - Нажать «Отменить» в течение 7 сек
   - Проверить: задача вернулась в исходную группу на исходную позицию
   - Проверить: статус задачи восстановлен (не `completed`)

4. **Стекирование toast'ов**
   - Быстро закрыть 3 задачи → проверить 3 toast'а на экране
   - Закрыть 4-ю задачу → проверить вытеснение самого старого toast'а, теперь 3 toast'а
   - Проверить: вытесненная задача **осталась** в «Завершённых» (не удалена)

5. **Смешанная очередь (delete + complete)**
   - Удалить задачу A → toast `delete`
   - Быстро закрыть задачи B, C → toast'ы `complete`
   - Закрыть задачу D → вытеснение oldest toast (задача A, `delete`) → A окончательно удаляется
   - Проверить: задачи B и C остались в «Завершённых»
   - Проверить: нажать undo для B → B вернулась в исходную группу

6. **Вытеснение complete-toast удалением**
   - Быстро закрыть задачи A, B, C → 3 toast'а `complete`
   - Удалить задачу D → вытесняется oldest `complete`-toast (задача A)
   - Проверить: задача A **осталась** в «Завершённых» (не удалена `finalDeleteTask`)

7. **Неактивная кнопка в «Завершённых»**
   - Развернуть группу «Завершённые»
   - Проверить: кнопка `☑` на карточках затемнена (opacity 0.3)
   - Проверить: клик не даёт эффекта
   - Проверить: tooltip «Задача уже завершена»

8. **Задача из Бэклога**
   - Создать задачу в Бэклоге (статус `new`)
   - Нажать `☑`
   - Проверить: задача в «Завершённых» со статусом `completed` (не `inProgress`)

9. **Задача со статусом `waiting`**
   - Создать задачу, открыть в модалке, поставить статус `waiting`
   - Нажать `☑` на карточке
   - Проверить: задача в «Завершённых» со статусом `completed`
   - Нажать undo → задача вернулась со статусом `waiting`

10. **Позиция в «Завершённых»**
    - Если в «Завершённых» уже есть задачи
    - Быстро закрыть новую задачу
    - Проверить: новая задача появилась **первой** в группе «Завершённых»

11. **Оба режима карточки — hover-видимость**
    - Обычный режим: навести на карточку → кнопки `☑` и `✕` появляются рядом в правом верхнем углу
    - Убрать курсор → кнопки скрываются
    - Компактный режим: кнопки `☑` и `✕` всегда видны

12. **Клик не открывает модалку**
    - Нажать `☑`
    - Проверить: модальное окно редактирования **не открывается** (`stopPropagation`)

13. **Закрытие Obsidian во время активного toast**
    - Нажать `☑`, не нажимать «Отменить»
    - Закрыть Obsidian (в течение 7 сек, пока toast активен)
    - Открыть Obsidian снова
    - Проверить: задача находится в «Завершённых» (persist сработал при нажатии)
    - Проверить: toast не появляется — undo недоступен после перезагрузки

14. **Локализация**
    - Переключить язык на EN → tooltip «Complete task», toast «Completed: ...»
    - Переключить на RU → tooltip «Завершить задачу», toast «Завершена: ...»

---

## Производительность

Операция `quickCompleteTask()` выполняет:
- 1 проход по `taskIds` исходной группы (O(n) где n — задачи в группе, обычно < 20)
- 1 `unshift` в `taskIds` «Завершённых» (O(m) где m — задачи в «Завершённых»)
- 1 вызов `persist()` (асинхронный, не блокирует UI)

Производительность эквивалентна операции drag-and-drop между группами. Проблем не ожидается.

---

## Известные ограничения

1. **Группа «Завершённые» не разворачивается автоматически** — пользователь не видит немедленного результата, если группа свёрнута. Компенсируется toast-уведомлением, подтверждающим действие.

2. **В обычном режиме кнопка скрыта до hover** — на touch-устройствах без мыши кнопка недоступна. Будет решено при мобильной адаптации (всегда видимые кнопки, как в компактном режиме).

3. **Нет визуальной анимации исчезновения карточки** — карточка пропадает мгновенно, без transition. Можно добавить в post-MVP.

4. **Undo восстанавливает позицию «как была»** — если после quick complete в той же группе закрыть другие задачи, позиция при undo может немного сместиться (существующее ограничение механизма delete undo).

---

## Дальнейшие улучшения (пост-MVP)

1. **Анимация исчезновения карточки** — плавный fade-out или slide-out при быстром закрытии (150-200ms)

2. **Автораскрытие «Завершённых»** — опциональная настройка: автоматически разворачивать группу после Quick Complete, чтобы пользователь видел результат

3. **Звуковое/вибро-подтверждение** — при будущей мобильной поддержке: тактильная обратная связь при закрытии задачи
