# Статус задачи «Звонок / Meeting» (Meeting Status)

---

# Часть 1: Продуктовая

## Что меняется для пользователя

- В списке статусов задачи появляется **новый статус «Звонок»** (EN: «Meeting»)
- При создании или редактировании задачи в выпадающем списке «Статус» доступен вариант «Звонок»
- На карточке задачи со статусом «Звонок» отображается иконка 📞 — визуально отличает задачу-встречу от рабочих задач
- Статус доступен **во всех группах** доски — бэклог, фокус, в работе, орг. намерения, делегировано, завершённые

---

## Зачем это нужно

При составлении дневного расписания в менеджере задач рабочие задачи и встречи/звонки перемешиваются. Визуально они выглядят одинаково, и пользователь не может быстро окинуть взглядом доску и понять, где нужно работать руками, а где — подключиться к созвону.

**Статус «Звонок» решает эту проблему:**
- Задача-встреча визуально отличается от рабочей задачи иконкой 📞
- При планировании дня пользователь сразу видит структуру: «3 рабочие задачи + 2 созвона»
- После завершения звонка задачу можно закрыть (Quick Complete или смена статуса), и она уходит в «Завершённые»

**Типичный сценарий:**
1. Утром пользователь создаёт задачи на день
2. Для встреч/созвонов выбирает статус «Звонок» — они помечаются 📞
3. Рабочие задачи остаются с обычными статусами (📋, 🛠️, ⏳)
4. Глядя на доску, пользователь мгновенно различает «работу» и «коммуникацию»
5. После созвона — закрывает задачу одним кликом (Quick Complete)

---

## Внешний вид на карточке

Иконка статуса отображается **рядом с иконкой приоритета**, как и для всех остальных статусов.

**Обычный режим карточки:**

```
┌────────────────────────────────────────────────────────────┐
│ 🟡 📞  📅 2026-02-18                             [☑]  [✕]  │
│ Синк с командой бэкенда                                    │
│ 👤 Петров А.                                               │
└────────────────────────────────────────────────────────────┘
```

**Компактный режим карточки:**

```
┌──────────────────────────────────────────────────────────┐
│ 🟡📞  Петров А.  Синк с командой бэкенда  📅 18  [☑] [✕] │
└──────────────────────────────────────────────────────────┘
```

---

## Поведение статуса

Статус «Звонок» — это маркер типа задачи. Он **не вводит специальных правил** и подчиняется общей логике:

| Действие | Поведение |
|----------|-----------|
| Создание задачи со статусом «Звонок» | Сохраняется как `meeting`, иконка 📞 |
| Перетаскивание между рабочими группами | Статус `meeting` **сохраняется** |
| Перетаскивание в «Завершённые» | Статус меняется на `completed` (общее правило) |
| Перетаскивание из «Завершённых» в рабочую группу | Статус меняется на `inProgress` (общее правило) |
| Quick Complete (кнопка ☑) | Статус → `completed`, как для любой задачи |
| Undo Quick Complete | Статус восстанавливается в `meeting` |
| Ручная смена статуса в модалке | Можно сменить на любой другой статус и обратно |

---

## Когда статус «Звонок» выбирается по умолчанию

**Никогда.** Статус по умолчанию для новых задач определяется группой: `new` для бэклога, `inProgress` для остальных. Пользователь выбирает «Звонок» вручную в форме создания/редактирования.

---

# Часть 2: Техническая

## Изменения в структуре данных

### `src/data/types.ts` — расширение типа `Status`

```typescript
// Было:
export type Status = 'new' | 'inProgress' | 'waiting' | 'completed';

// Стало:
export type Status = 'new' | 'inProgress' | 'waiting' | 'meeting' | 'completed';
```

### Миграция данных

Изменений в схеме `data.json` **нет**. Поле `status` в модели `Task` уже существует как строка. Новое значение `'meeting'` — это расширение допустимых значений типа, не изменение структуры. Миграция не требуется.

У существующих задач статус не меняется. Новый статус `meeting` применяется только к вновь создаваемым или вручную изменённым задачам.

---

## Изменения в компонентах

### 1. `src/data/types.ts` — тип Status

Добавить `'meeting'` в union-тип `Status` (между `'waiting'` и `'completed'` для логичного порядка):

```typescript
export type Status = 'new' | 'inProgress' | 'waiting' | 'meeting' | 'completed';
```

---

### 2. `src/ui/TaskCard.svelte` — иконка статуса

Добавить `meeting: '📞'` в маппинг `statusIcons`.

**Замечание:** `statusIcons` типизирован как `Record<string, string>` с fallback `?? ''`. Если забыть добавить ключ `meeting` — TypeScript не поймает ошибку, просто иконка не отобразится. При реализации убедиться, что ключ добавлен.

```typescript
const statusIcons: Record<string, string> = {
  new: '📋',
  inProgress: '🛠️',
  waiting: '⏳',
  meeting: '📞',
  completed: '✅',
};
```

---

### 3. `src/ui/TaskFormContent.svelte` — опция в выпадающем списке

Добавить новый `<option>` в `<select id="tm-status">` перед `completed`:

```svelte
<select id="tm-status" class="tm-task-form__select" bind:value={status}>
  <option value="new">{$t('status.new')}</option>
  <option value="inProgress">{$t('status.inProgress')}</option>
  <option value="waiting">{$t('status.waiting')}</option>
  <option value="meeting">{$t('status.meeting')}</option>
  <option value="completed">{$t('status.completed')}</option>
</select>
```

---

### 4. `src/logic/statusTransitions.ts` — без изменений

Текущая логика автоматических переходов уже корректно обрабатывает статус `meeting`:

- **Из бэклога в рабочую группу:** переход `new` → `inProgress` срабатывает только для `status === 'new'`. Задача со статусом `meeting` сохраняет свой статус — правильно.
- **В «Завершённые»:** любой статус → `completed` — правильно.
- **Из «Завершённых» в рабочую:** `completed` → `inProgress` — стандартное поведение, работает.
- **Между рабочими группами:** статус не меняется — правильно.

Изменений в `statusTransitions.ts` не требуется.

---

### 5. `src/i18n/types.ts` — новый ключ перевода

Добавить в `TranslationKey`:

```typescript
| 'status.meeting'
```

---

### 6. `src/i18n/en.ts` — английский перевод

Добавить после `'status.waiting'`:

```typescript
'status.meeting': 'Meeting',
```

---

### 7. `src/i18n/ru.ts` — русский перевод

Добавить после `'status.waiting'`:

```typescript
'status.meeting': 'Звонок',
```

---

## Затронутые файлы (сводка)

| Файл | Тип изменения |
|------|---------------|
| `src/data/types.ts` | Расширить union-тип `Status` |
| `src/ui/TaskCard.svelte` | Добавить иконку в `statusIcons` |
| `src/ui/TaskFormContent.svelte` | Добавить `<option>` в `<select>` |
| `src/i18n/types.ts` | Добавить ключ `'status.meeting'` |
| `src/i18n/en.ts` | Добавить перевод |
| `src/i18n/ru.ts` | Добавить перевод |

**Не затрагиваются:** `statusTransitions.ts`, `defaults.ts`, `migration.ts`, `dataStore.ts`, `styles.css`.

---

## Тестирование

### Функциональные сценарии

1. **Создание задачи со статусом «Звонок»**
   - Открыть модалку «Новая задача» в любой группе
   - В выпадающем списке «Статус» выбрать «Звонок» (EN: «Meeting»)
   - Сохранить задачу
   - Проверить: на карточке отображается иконка 📞

2. **Редактирование существующей задачи**
   - Открыть задачу с любым статусом
   - Сменить статус на «Звонок»
   - Сохранить → иконка на карточке стала 📞
   - Открыть снова → в выпадающем списке выбрано «Звонок»

3. **Смена статуса обратно**
   - Открыть задачу со статусом «Звонок»
   - Сменить на «В работе»
   - Сохранить → иконка стала 🛠️

4. **Drag-and-drop между рабочими группами**
   - Создать задачу со статусом «Звонок» в группе «Фокус»
   - Перетащить в «В работе»
   - Проверить: иконка всё ещё 📞 (статус `meeting` сохранён)

5. **Drag-and-drop в «Завершённые»**
   - Перетащить задачу со статусом «Звонок» в «Завершённые»
   - Проверить: иконка стала ✅, статус `completed`

6. **Drag-and-drop из «Завершённых» обратно**
   - Перетащить задачу из «Завершённых» в рабочую группу
   - Проверить: статус стал `inProgress`, иконка 🛠️ (стандартное поведение)

7. **Quick Complete задачи со статусом «Звонок»**
   - Нажать ☑ на карточке с 📞
   - Проверить: задача перешла в «Завершённые» со статусом `completed`
   - Нажать «Отменить» → задача вернулась со статусом `meeting`, иконка 📞

8. **Задача «Звонок» в бэклоге**
   - Создать задачу в бэклоге, поменять статус на «Звонок»
   - Перетащить в рабочую группу
   - Проверить: статус остался `meeting` (не сменился на `inProgress`, т.к. условие только для `new`)

9. **Компактный режим карточки**
   - Переключить в Settings → Card view: Compact
   - Проверить: иконка 📞 корректно отображается в компактном режиме

10. **Локализация**
    - EN: в выпадающем списке — «Meeting»
    - RU: в выпадающем списке — «Звонок»

---

## Известные ограничения

1. **Задача-встреча с прошедшим дедлайном подсвечивается как просроченная** — логика `isOverdue` в `TaskCard.svelte` проверяет `task.status !== 'completed'`, поэтому встреча с прошедшей датой получит красный индикатор. Это ожидаемое поведение: пользователь должен закрыть задачу после встречи, как и любую другую.

2. **При возврате из «Завершённых» drag-and-drop'ом статус становится `inProgress`** — пользователь теряет маркер «Звонок». Это общее поведение для всех нестандартных статусов. Можно улучшить в будущем, сохраняя предыдущий статус при возврате из completed.

3. **Нет визуального отличия кроме иконки** — карточка задачи-встречи не имеет особого фона или бордера. Для MVP достаточно иконки; стилизацию можно добавить позже.

---

## Дальнейшие улучшения (пост-MVP)

1. **Визуальное выделение карточки** — лёгкий цветной бордер или фон для задач со статусом `meeting`, чтобы ещё сильнее отличать их от рабочих задач

2. **Сохранение статуса при возврате из «Завершённых»** — при перетаскивании задачи обратно из completed восстанавливать предыдущий статус (не только для `meeting`, но для всех статусов)

3. **Время встречи** — отдельное поле с временем начала/окончания звонка (часы:минуты), чтобы задачи-встречи можно было сортировать по времени в рамках дня
