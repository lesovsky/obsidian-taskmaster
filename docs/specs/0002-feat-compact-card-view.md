# Компактный вид карточек задач

---

# Часть 1: Продуктовая

## Что меняется для пользователя

- Добавляется **компактный однострочный режим** отображения карточек задач
- Переключение между обычным (трёхстрочным) и компактным (однострочным) видом через **глобальные настройки плагина**
- Все задачи на всех досках переключаются одновременно в выбранный режим
- Изменение применяется мгновенно, без перезагрузки

---

## Зачем это нужно

В повседневной работе проектного менеджера может накапливаться много операционных задач. При большом количестве карточек текущий трёхстрочный формат:

```
┌─────────────────────────┐
│ 🔴 ⏳       2026-03-24 │
│ Подготовить отчёт для   │
│ совета директоров       │
│ 👤 Иванов А.            │
└─────────────────────────┘
```

занимает много вертикального пространства, что требует частой прокрутки групп и затрудняет обзор всех задач.

**Компактный вид решает эту проблему**, размещая всю информацию в одну строку:

```
┌────────────────────────────────────────────────┐
│ 🔴 ⏳  Иванов А.  Подготовить отчёт...  📅 24 │
└────────────────────────────────────────────────┘
```

Это позволяет:
- Видеть больше задач одновременно без прокрутки
- Быстрее сканировать список глазами
- Эффективнее работать с большим количеством операционных задач

---

## Текущий вид (трёхстрочный)

Карточка занимает **3 строки** сверху вниз:

1. **Строка 1:** Иконки статуса и приоритета (слева) + дедлайн (справа)
2. **Строка 2:** Описание задачи ("Что нужно сделать")
3. **Строка 3:** Исполнитель ("Кто")

```
┌─────────────────────────────────────┐
│ 🔴 ⏳                    📅 2026-03-24 │
│ Подготовить отчёт для совета        │
│ директоров по итогам Q1             │
│ 👤 Иванов А.                        │
└─────────────────────────────────────┘
```

**Кнопка удаления:** отображается в правом верхнем углу (при наведении курсора).

---

## Компактный вид (однострочный)

Карточка занимает **1 строку** слева направо:

1. **Секция иконок** (фиксированная ширина) — статус и приоритет
2. **Исполнитель** ("Кто") — фиксированная ширина, обрезается многоточием при переполнении
3. **Описание задачи** ("Что") — занимает оставшееся пространство, обрезается многоточием при переполнении
4. **Дедлайн** (если указан) — фиксированная ширина справа
5. **Кнопка удаления** — в крайней правой позиции

```
┌─────────────────────────────────────────────────────┐
│ 🔴⏳  Иванов А.  Подготовить отчёт...  📅 24  ✕    │
└─────────────────────────────────────────────────────┘
```

**Визуальные разделители:** между секциями используются только CSS-отступы (gap), без вертикальных линий или символов.

**Примечание:** В компактном виде иконка 👤 перед исполнителем **не отображается** (в отличие от обычного вида), чтобы сэкономить горизонтальное пространство. Иконка 📅 перед дедлайном сохраняется для визуальной идентификации.

---

## Обработка граничных случаев

### Случай 1: Нет исполнителя (поле "Кто" пустое)

Секция исполнителя **пропускается**, описание начинается сразу после иконок:

```
┌──────────────────────────────────────────┐
│ 🔴⏳  Написать тесты для API...  📅 25  ✕ │
└──────────────────────────────────────────┘
```

### Случай 2: Нет дедлайна (поле "Когда" пустое)

Секция дедлайна не отображается, описание занимает всё доступное пространство до кнопки удаления:

```
┌──────────────────────────────────────────────┐
│ 🟡🛠️  Петров Б.  Провести код-ревью...   ✕ │
└──────────────────────────────────────────────┘
```

### Случай 3: Длинное имя исполнителя

Поле "Кто" имеет **фиксированную ширину** (~100-120px). Длинные имена обрезаются с многоточием:

```
┌─────────────────────────────────────────────────┐
│ 🟢📋  Константинопо...  Обновить...  📅 28  ✕ │
└─────────────────────────────────────────────────┘
```

### Случай 4: Длинное описание задачи

Поле "Что" занимает оставшееся пространство между исполнителем и дедлайном. При переполнении текст обрезается с многоточием:

```
┌──────────────────────────────────────────────────┐
│ 🔴⏳  Иванов А.  Подготовить детальный...  📅 24  ✕ │
└──────────────────────────────────────────────────┘
```

**Важно:** обрезка происходит так, чтобы текст не упирался в дедлайн — всегда сохраняется минимальный отступ.

### Случай 5: Нет ни исполнителя, ни дедлайна

Отображаются только иконки и описание:

```
┌─────────────────────────────────────────────┐
│ 🟡📋  Добавить логирование в сервис...   ✕ │
└─────────────────────────────────────────────┘
```

### Случай 6: Просроченная задача

Карточка получает красную рамку (как в обычном виде), дедлайн выделяется красным цветом и жирным шрифтом:

```
┌─────────────────────────────────────────────────┐  ← красная рамка
│ 🔴⏳  Сидоров В.  Исправить баг...  📅 12  ✕  │  ← дедлайн красный
└─────────────────────────────────────────────────┘
```

### Случай 7: Пустая группа (Empty State)

Текст-подсказка в пустой группе отображается **идентично обычному виду** — многострочным текстом. Режим отображения карточек не влияет на Empty State.

```
┌──────────────────────────────────────────────────┐
│ Что сейчас требует вашего внимания?              │
│ Перетащите сюда задачи, которые нельзя           │
│ упускать из виду                                 │
└──────────────────────────────────────────────────┘
```

**Обоснование:** Подсказки предназначены для объяснения назначения группы новым пользователям. Они отображаются редко (только в пустых группах), поэтому не влияют на плотность информации и не требуют сокращения.

---

## Настройка режима отображения

В **Settings → TaskMaster** появляется новый параметр **Card view** (режим отображения карточек), расположенный после настройки языка:

| Значение | Поведение |
|----------|-----------|
| **Default** (по умолчанию) | Трёхстрочный формат — иконки и дедлайн в первой строке, описание во второй, исполнитель в третьей |
| **Compact** | Однострочный формат — все элементы в одну строку слева направо |

**Переключение:**
- Изменение настройки **немедленно перерисовывает** все карточки на всех досках
- Режим применяется **глобально** ко всем доскам плагина
- Изменение сохраняется в `data.json` и восстанавливается при следующем запуске Obsidian

---

## Когда использовать каждый режим

### Default (трёхстрочный) — рекомендуется когда:
- Задач в группах немного (3-7 штук)
- Важно видеть полный текст описания и исполнителя
- Работаете со стратегическими, долгосрочными задачами
- Предпочитаете визуальный комфорт и читаемость

### Compact (однострочный) — рекомендуется когда:
- Задач много (10+ в группе)
- Нужен быстрый обзор всех задач без прокрутки
- Работаете с операционными, короткими задачами
- Важна плотность информации на экране

**Компромиссы компактного режима:**
- **Скрыто поле "Зачем" (Why)** — важная часть SMART-методологии недоступна визуально. Увидеть цель задачи можно только открыв модалку редактирования. Если поле "Зачем" критично для вашего workflow, рассмотрите использование обычного режима.
- **Изменяется восприятие WIP-лимитов** — в компактном виде в группу визуально поместится в 2-3 раза больше карточек без прокрутки. Группа с 8 задачами при лимите 5 в обычном виде требует прокрутки (визуально "переполнена"), а в компактном виде все 8 задач помещаются на экране (визуально "нормально"). Это может повлиять на дисциплину соблюдения WIP-лимитов.

Режим можно переключать в любой момент в зависимости от текущего контекста работы.

---

## Визуальная спецификация

### Ширины секций в компактном виде

| Секция | Ширина | Обрезка | Примечание |
|--------|--------|---------|------------|
| **Иконки** | Фиксированная 2.5rem (~40px) | Нет | Всегда 2 иконки (приоритет + статус), фиксированный размер для выравнивания |
| **Исполнитель** | Фиксированная 7rem (~112px) | Многоточием | Только если поле заполнено. При обрезке показывается полный текст через tooltip |
| **Описание** | Flex-grow (остаток) | Многоточием | Занимает всё свободное пространство. При обрезке показывается полный текст через tooltip |
| **Дедлайн** | Фиксированная 4.5rem (~72px) | Нет | Только если указан, формат «📅 DD» / «📅 MM-DD» / «📅 DD 'YY» (см. ниже) |
| **Кнопка удаления** | Фиксированная 1.25rem (~20px) | Нет | Всегда видна в крайней правой позиции |

**Примечание о единицах измерения:**
- Используются `rem` для адаптивности под настройки темы пользователя
- При базовом размере шрифта 16px: 1rem = 16px
- **Минимальная ширина группы** для корректного отображения компактного вида: **320px** (20rem)
- При ширине группы менее 320px компактный вид может отображаться некорректно (описание задачи может исчезнуть)

### CSS-отступы между секциями

- Между иконками и исполнителем: `0.5rem`
- Между исполнителем и описанием: `0.5rem`
- Между описанием и дедлайном: `0.5rem`
- Между дедлайном и кнопкой удаления: `0.25rem`

### Высота карточки в компактном виде

- Минимальная высота: `1.75rem` (~28px при стандартном размере шрифта)
- Фактическая высота зависит от контента и padding: обычно `~32-36px`
- Достаточная для комфортного чтения текста размером 0.85rem на desktop

**Важно для мобильной адаптации:**
- Рекомендованный минимум для touch targets: **44px** (iOS HIG) / **48px** (Material Design)
- Текущая высота (~32-36px) **ниже** этого стандарта
- **Рекомендация:** Компактный режим **не рекомендуется** для использования на мобильных устройствах
- При будущей реализации мобильной версии компактный режим должен либо:
  - Автоматически отключаться на мобильных (fallback на Default)
  - Использовать увеличенную высоту карточек (min-height: 2.75rem / ~44px) на touch-устройствах

---

## Доступность (Accessibility)

Компактный режим вводит изменения, влияющие на доступность интерфейса:

### 1. Размер текста
- Исполнитель: `0.75rem` (меньше стандартного)
- Дедлайн: `0.75rem`
- Описание: `0.85rem` (стандартное)

**Требование:** Минимальный контраст для текста <0.875rem должен быть **4.5:1** (WCAG AA для мелкого текста). Используем CSS-переменные Obsidian (`--text-muted`, `--text-normal`), которые обеспечивают правильный контраст в любой теме.

### 2. Размер кликабельных областей
- Высота карточки: ~32-36px (ниже рекомендованных 44px для touch)
- Кнопка удаления: 1.25rem × 1.25rem (~20px × 20px, ниже рекомендованных 44px × 44px)

**Компенсация:**
- Клик по всей карточке открывает модалку редактирования (большая область взаимодействия)
- Drag-and-drop работает по всей площади карточки
- Кнопка удаления имеет увеличенную padding-область для клика

### 3. Навигация с клавиатуры
- Карточка доступна через Tab (`tabindex="0"`)
- Enter открывает модалку редактирования
- Индикатор фокуса (`:focus-visible`) должен быть хорошо заметен на компактных карточках

**Стили для индикатора фокуса:**
```css
.tm-task-card--compact:focus-visible {
  outline: 2px solid var(--interactive-accent);
  outline-offset: 2px;
}
```

### 4. Tooltips для обрезанного контента
- HTML-атрибут `title` показывает полный текст при наведении
- Исполнитель: `title={task.who}`
- Описание: `title={task.what}`

**Ограничение:** Нативные tooltips не работают на touch-устройствах. На мобильных единственный способ увидеть полный текст — открыть карточку.

### 5. Скринридеры
Структура карточки сохраняет семантику:
- `role="button"` — карточка объявляется как кнопка
- Порядок элементов в DOM соответствует визуальному порядку (слева направо)
- Текст читается в правильной последовательности: иконки → исполнитель → описание → дедлайн

**Рекомендация для будущих улучшений:** Добавить `aria-label` с полным текстом задачи для лучшей навигации со скринридерами.

---

## Особенности взаимодействия

### Drag-and-drop
Работает идентично обычному виду — карточку можно перетаскивать, захватив за любую её часть (кроме кнопки удаления).

### Клик по карточке
Открывает модальное окно редактирования со всеми полями задачи (Что, Зачем, Кто, Когда, Приоритет, Статус).

### Кнопка удаления
Расположена справа, всегда видна (не скрывается при отсутствии hover). Клик удаляет задачу с появлением toast-уведомления «Отменить».

**Важное отличие от обычного вида:** В обычном режиме кнопка удаления скрыта до наведения курсора (`opacity: 0`), в компактном — всегда видна (`opacity: 1`).

**Риск случайных кликов:** При плотном расположении карточек увеличивается вероятность случайно кликнуть на "✕" при попытке захватить карточку для drag-and-drop.

**Защита:** Механизм отмены удаления (toast с кнопкой «Отменить», 7 секунд) защищает от потери данных при случайном клике.

### Просрочка
Визуально подсвечивается красной рамкой карточки + красным цветом дедлайна (как в обычном виде).

---

# Часть 2: Техническая

## Изменения в структуре данных

### data.json — новое поле настроек

Добавляется поле `cardView` в объект `settings`:

```json
{
  "version": 1,
  "settings": {
    "defaultPriority": "medium",
    "language": "auto",
    "cardView": "default"  // ← новое поле
  },
  "boards": [...],
  "tasks": {...}
}
```

**Возможные значения:**
- `"default"` — трёхстрочный вид (значение по умолчанию)
- `"compact"` — однострочный вид

### TypeScript — обновление типа Settings

```typescript
// src/data/types.ts
interface Settings {
  defaultPriority: Priority;
  language: 'auto' | 'en' | 'ru';
  cardView: 'default' | 'compact';  // ← новое поле
}
```

### Дефолтное значение

```typescript
// src/data/defaults.ts
export const DEFAULT_SETTINGS: Settings = {
  defaultPriority: 'medium',
  language: 'auto',
  cardView: 'default',  // ← новое поле
};
```

---

## Изменения в компонентах

### 1. TaskCard.svelte — условный рендеринг

Компонент получает информацию о текущем режиме отображения из `dataStore` и рендерит соответствующую разметку:

```svelte
<script lang="ts">
  import type { Task } from '../data/types';
  import { dataStore } from '../stores/dataStore';
  import { t } from '../i18n';

  export let task: Task;
  export let onClick: () => void;
  export let onDelete: () => void;

  $: cardView = $dataStore.settings.cardView;
  $: isCompact = cardView === 'compact';

  // ... существующая логика иконок, просрочки
</script>

{#if isCompact}
  <!-- Компактный вид -->
  <div
    class="tm-task-card tm-task-card--compact"
    class:tm-task-card--overdue={isOverdue}
    data-task-id={task.id}
    on:click={onClick}
    role="button"
    tabindex="0"
  >
    <div class="tm-task-card__icons">
      <span class="tm-task-card__priority">{icon}</span>
      <span class="tm-task-card__status">{statusIcon}</span>
    </div>
    {#if task.who}
      <div class="tm-task-card__who-compact" title={task.who}>{task.who}</div>
    {/if}
    <div class="tm-task-card__what-compact" title={task.what}>{task.what}</div>
    {#if task.deadline}
      <span class="tm-task-card__deadline-compact" class:tm-task-card__deadline--overdue={isOverdue}>
        📅 {formatDeadlineShort(task.deadline)}
      </span>
    {/if}
    <button
      class="tm-task-card__delete-compact clickable-icon"
      on:click|stopPropagation={onDelete}
      title={$t('taskCard.deleteTask')}
    >✕</button>
  </div>
{:else}
  <!-- Обычный вид (текущая реализация) -->
  <div
    class="tm-task-card"
    class:tm-task-card--overdue={isOverdue}
    data-task-id={task.id}
    on:click={onClick}
    role="button"
    tabindex="0"
  >
    <div class="tm-task-card__top">
      <span class="tm-task-card__priority">{icon}</span>
      <span class="tm-task-card__status">{statusIcon}</span>
      {#if task.deadline}
        <span class="tm-task-card__deadline" class:tm-task-card__deadline--overdue={isOverdue}>
          📅 {task.deadline}
        </span>
      {/if}
    </div>
    <div class="tm-task-card__what">{task.what}</div>
    {#if task.who}
      <div class="tm-task-card__who">👤 {task.who}</div>
    {/if}
    <button
      class="tm-task-card__delete clickable-icon"
      on:click|stopPropagation={onDelete}
      title={$t('taskCard.deleteTask')}
    >✕</button>
  </div>
{/if}
```

**Функция форматирования короткого дедлайна:**

```typescript
// src/utils/dateFormat.ts
export function formatDeadlineShort(dateStr: string): string {
  // "2026-03-24" → "24" (если текущий месяц и год)
  // "2026-03-24" → "03-24" (если другой месяц, но текущий год)
  // "2027-03-24" → "24 '27" (если другой год)
  const today = new Date();
  const deadline = new Date(dateStr);

  const isSameYear = today.getFullYear() === deadline.getFullYear();
  const isSameMonth = today.getMonth() === deadline.getMonth();

  // Если текущий месяц и год — показываем только день
  if (isSameYear && isSameMonth) {
    return deadline.getDate().toString();
  }

  const day = deadline.getDate().toString().padStart(2, '0');
  const month = (deadline.getMonth() + 1).toString().padStart(2, '0');

  // Если текущий год — показываем месяц-день
  if (isSameYear) {
    return `${month}-${day}`;
  }

  // Если другой год — показываем день и сокращённый год
  const yearShort = deadline.getFullYear().toString().slice(-2);
  return `${day} '${yearShort}`;
}
```

**Примеры форматирования:**
- Сегодня 16 февраля 2026, дедлайн 24 февраля 2026 → `"24"`
- Сегодня 16 февраля 2026, дедлайн 24 марта 2026 → `"03-24"`
- Сегодня 16 февраля 2026, дедлайн 24 марта 2027 → `"24 '27"`
- Сегодня 16 февраля 2026, дедлайн 12 января 2025 (просрочен) → `"12 '25"` + красная подсветка

---

### 2. styles.css — стили для компактного вида

```css
/* Компактный вид карточки */
.tm-task-card--compact {
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 0.5rem;
  padding: 0.35rem 0.5rem;
  min-height: 1.75rem;
  height: auto;
}

/* Индикатор фокуса для навигации с клавиатуры */
.tm-task-card--compact:focus-visible {
  outline: 2px solid var(--interactive-accent);
  outline-offset: 2px;
}

/* Секция иконок — фиксированная ширина */
.tm-task-card__icons {
  display: flex;
  align-items: center;
  gap: 0.25rem;
  flex-shrink: 0;
  width: 2.5rem;  /* фиксированная ширина для выравнивания */
}

/* Исполнитель в компактном виде — фиксированная ширина, обрезка */
.tm-task-card__who-compact {
  flex-shrink: 0;
  width: 7rem;  /* ~112px при базовом размере шрифта 16px */
  font-size: 0.75rem;
  color: var(--text-muted);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* Описание в компактном виде — flex-grow, обрезка */
.tm-task-card__what-compact {
  flex: 1;
  font-size: 0.85rem;
  color: var(--text-normal);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  min-width: 0;  /* важно для корректной работы text-overflow в flex */
}

/* Дедлайн в компактном виде — фиксированная ширина */
.tm-task-card__deadline-compact {
  flex-shrink: 0;
  width: 4.5rem;  /* ~70px */
  font-size: 0.75rem;
  color: var(--text-muted);
  text-align: right;
}

/* Кнопка удаления в компактном виде */
.tm-task-card__delete-compact {
  flex-shrink: 0;
  width: 1.25rem;
  height: 1.25rem;
  font-size: 0.7rem;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  border: none;
  background: transparent;
  color: var(--text-faint);
  cursor: pointer;
  opacity: 1;  /* всегда видна */
}

.tm-task-card__delete-compact:hover {
  background: var(--background-modifier-hover);
  color: var(--text-error);
}
```

---

### 3. settings.ts — новая настройка в UI

Добавляется dropdown после настройки языка:

```typescript
// src/settings.ts
import { App, PluginSettingTab, Setting } from 'obsidian';
import type TaskMasterPlugin from './main';
import { t } from './i18n';

export class TaskMasterSettingTab extends PluginSettingTab {
  plugin: TaskMasterPlugin;

  constructor(app: App, plugin: TaskMasterPlugin) {
    super(app, plugin);
    this.plugin = plugin;
  }

  display(): void {
    const { containerEl } = this;
    containerEl.empty();

    // ... существующая настройка языка

    // Новая настройка: режим отображения карточек
    new Setting(containerEl)
      .setName(t('settings.cardView'))
      .setDesc(t('settings.cardViewDesc'))
      .addDropdown((dropdown) =>
        dropdown
          .addOption('default', t('settings.cardViewDefault'))
          .addOption('compact', t('settings.cardViewCompact'))
          .setValue(this.plugin.settings.cardView)
          .onChange(async (value) => {
            this.plugin.settings.cardView = value as 'default' | 'compact';
            await this.plugin.saveSettings();
            // Уведомляем dataStore об изменении настроек
            this.plugin.dataStore.updateSettings({ cardView: value as 'default' | 'compact' });
          })
      );

    // Примечание: saveSettings() должен автоматически обновлять dataStore,
    // поэтому явный вызов updateSettings() может быть избыточен.
    // Проверить, не приводит ли это к двойному сохранению.

    // ... остальные настройки
  }
}
```

---

### 4. dataStore.ts — метод обновления настроек

```typescript
// src/stores/dataStore.ts
import { writable } from 'svelte/store';
import type { PluginData, Settings } from '../data/types';

function createDataStore() {
  const { subscribe, set, update } = writable<PluginData>({} as PluginData);

  return {
    subscribe,
    set,
    update,

    // Метод для обновления настроек (триггерит реактивность)
    updateSettings(newSettings: Partial<Settings>) {
      update(data => ({
        ...data,
        settings: { ...data.settings, ...newSettings }
      }));
    },

    // ... остальные методы
  };
}

export const dataStore = createDataStore();
```

---

### 5. i18n — новые ключи локализации

```typescript
// src/i18n/en.ts
export const en = {
  // ... существующие ключи
  settings: {
    language: 'Language',
    languageDesc: 'Interface language',
    cardView: 'Card view',
    cardViewDesc: 'Choose how task cards are displayed',
    cardViewDefault: 'Default (3 lines)',
    cardViewCompact: 'Compact (1 line)',
    // ...
  },
};

// src/i18n/ru.ts
export const ru = {
  // ... существующие ключи
  settings: {
    language: 'Язык',
    languageDesc: 'Язык интерфейса',
    cardView: 'Вид карточек',
    cardViewDesc: 'Как отображать карточки задач',
    cardViewDefault: 'Обычный (3 строки)',
    cardViewCompact: 'Компактный (1 строка)',
    // ...
  },
};
```

**Важно:** Ключи для значений dropdown изменены на `cardViewDefault` и `cardViewCompact` (вместо вложенного объекта `cardView: {}`), чтобы избежать конфликта имён.

---

## Миграция данных

Поле `cardView` добавляется в `settings` при первой загрузке версии с этой фичей. Миграция не требуется, так как значение по умолчанию (`'default'`) обеспечивает обратную совместимость.

При загрузке плагина:

```typescript
// src/main.ts
async loadPluginData() {
  const data = await this.loadData();
  if (data) {
    // Миграция данных (если нужна)
    const migrated = migrateData(data);

    // Убедиться, что settings.cardView существует
    if (!migrated.settings.cardView) {
      migrated.settings.cardView = 'default';
    }

    this.dataStore.set(migrated);
  } else {
    // Первый запуск — использовать дефолтные значения
    this.dataStore.set(getDefaultPluginData());
  }
}
```

---

## Производительность

### Реактивность Svelte
Изменение `settings.cardView` в `dataStore` автоматически триггерит перерисовку всех компонентов `TaskCard.svelte` на всех досках. Svelte эффективно обновляет только изменившиеся узлы DOM.

### Потенциальная проблема
При большом количестве задач (100+) одновременная перерисовка всех карточек может вызвать задержку UI.

### Решение (опционально для post-MVP)
Если возникнут проблемы с производительностью:
- Добавить debounce на изменение настройки (300ms задержка перед применением)
- Использовать виртуализацию списков (`svelte-virtual` или аналог) для групп с большим количеством задач

---

## Тестирование

### Сценарии тестирования

1. **Переключение режима**
   - Открыть Settings → TaskMaster → Card view
   - Переключить между Default и Compact
   - Проверить, что все карточки на всех досках мгновенно меняют вид

2. **Граничные случаи в компактном виде**
   - Карточка без исполнителя (поле "Кто" пустое)
   - Карточка без дедлайна (поле "Когда" пустое)
   - Карточка без исполнителя и дедлайна
   - Длинное имя исполнителя (>20 символов)
   - Длинное описание задачи (>100 символов)

3. **Просрочка в компактном виде**
   - Создать задачу с дедлайном в прошлом
   - Проверить красную рамку карточки
   - Проверить красный цвет дедлайна

4. **Drag-and-drop в компактном виде**
   - Перетащить карточку внутри группы
   - Перетащить карточку между группами
   - Проверить, что позиция сохраняется

5. **Модальное окно редактирования**
   - Кликнуть по компактной карточке
   - Проверить, что открывается модалка со всеми полями
   - Изменить поля, сохранить
   - Проверить, что изменения отображаются в компактном виде

6. **Удаление в компактном виде**
   - Нажать кнопку удаления
   - Проверить, что появляется toast с кнопкой «Отменить»
   - Проверить отмену удаления

7. **Локализация**
   - Переключить язык интерфейса на русский/английский
   - Проверить, что названия настроек переведены
   - Проверить, что dropdown показывает правильные значения

8. **Сохранение настройки**
   - Переключить на Compact
   - Перезагрузить Obsidian
   - Проверить, что карточки остались в компактном виде

9. **Tooltips для обрезанного текста**
   - Создать задачу с длинным исполнителем (>20 символов)
   - Создать задачу с длинным описанием (>100 символов)
   - В компактном виде навести курсор на обрезанный текст
   - Проверить, что появляется tooltip с полным текстом

10. **Навигация с клавиатуры**
   - Использовать Tab для навигации между карточками
   - Проверить, что индикатор фокуса (outline) хорошо виден
   - Нажать Enter на сфокусированной карточке
   - Проверить, что открывается модалка редактирования

11. **Минимальная ширина группы**
   - Уменьшить ширину окна Obsidian или сделать узкую раскладку панелей
   - Проверить, что при ширине группы <320px компактный вид начинает "ломаться"
   - Проверить, что при нормальной ширине всё отображается корректно

12. **Форматирование дедлайна с годом**
   - Создать задачу с дедлайном в текущем месяце (показывается только день)
   - Создать задачу с дедлайном в другом месяце текущего года (показывается MM-DD)
   - Создать задачу с дедлайном в другом году (показывается DD 'YY)
   - Проверить все три формата в компактном виде

---

## Обратная совместимость

- Пользователи с `data.json` без поля `cardView` автоматически получают значение `'default'`
- Существующие карточки продолжают работать без изменений
- При откате на старую версию плагина (без фичи) поле `cardView` игнорируется, карточки отображаются в обычном виде

---

## Известные ограничения

1. **Поле "Зачем" (Why) скрыто** — ключевая часть SMART-методологии недоступна визуально в компактном режиме. Пользователи должны открывать модалку для просмотра цели задачи.

2. **Высота карточек ниже стандарта touch-интерфейсов** — ~32-36px против рекомендованных 44-48px. Компактный режим не оптимален для мобильных устройств.

3. **Tooltips не работают на touch-устройствах** — на мобильных нет способа увидеть полный обрезанный текст без открытия карточки.

4. **Риск случайных кликов по кнопке удаления** — кнопка всегда видна, увеличивается вероятность случайного удаления при drag-and-drop (частично компенсируется механизмом отмены).

5. **Зависимость размеров от темы** — использование `rem` делает фактические размеры зависимыми от базового размера шрифта темы (14-18px в разных темах Obsidian).

6. **Минимальная ширина группы** — при ширине <320px компактный вид отображается некорректно (описание может исчезнуть).

7. **Форматирование дедлайна без локализации** — формат "03-24" (месяц-день) привычен для США, но не для большинства других регионов, где принят формат "24-03" (день-месяц). Текущая версия не учитывает локаль пользователя.

---

## Дальнейшие улучшения (пост-MVP)

1. **Per-board настройка** — возможность выбрать режим отображения для каждой доски отдельно

2. **Кастомизация ширин** — пользовательские настройки ширины секций в компактном виде (для продвинутых пользователей)

3. **Дополнительные режимы**:
   - **Ultra-compact** — только иконки + описание (без исполнителя и дедлайна)
   - **Medium** — 2 строки (первая: иконки + исполнитель + дедлайн, вторая: описание)

4. **Индикатор поля "Зачем"** — показывать иконку 💡 или точку, если поле "Why" заполнено, чтобы пользователь знал о наличии описания цели

5. **Горячие клавиши** — быстрое переключение режима через Command Palette (например, "Toggle compact card view")

6. **Виртуализация списков** — для оптимизации отображения групп с 50+ задачами (библиотеки: `svelte-virtual` или `@tanstack/virtual`)

7. **Адаптивная высота для мобильных** — автоматическое увеличение `min-height` до 2.75rem (~44px) на touch-устройствах

8. **Локализация формата дат** — учитывать локаль пользователя (день-месяц для EU/RU, месяц-день для US)

9. **CSS-анимация переключения режимов** — плавная анимация изменения высоты карточек (100-150ms transition) для более комфортного визуального перехода

10. **Индикатор обрезанного текста** — визуальная подсказка (например, градиент fade-out), что текст обрезан, помимо стандартного многоточия
