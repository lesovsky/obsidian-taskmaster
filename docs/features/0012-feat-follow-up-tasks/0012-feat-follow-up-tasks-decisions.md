# Decisions Log: 0012 Follow-up tasks

Отчёты о выполнении задач. Одна запись на задачу, добавляется исполнителем.

---

## Task 01: Localization keys

**Commit:** eddb7aa
**Summary:** Добавлены десять ключей `followUps.*` в union `TranslationKey` и в оба словаря (en/ru) одним блоком после `notes.*`, тексты — ровно по таблице задачи. Интерполяция в i18n не добавлялась: подстановка `{group}`/`{count}` остаётся за чистым форматтером Task 03 (Decision 7). Ревью: [dev-code-reviewer](0012-feat-follow-up-tasks-task-01-dev-code-reviewer-review.json) — approved_with_suggestions, одно minor-замечание.
**Deviations:** Нет в коде. Отклонено замечание ревьюера: ru `followUps.toBacklogHint` = «Будет заведена в бэклог при сохранении» (женский род — подразумевается «задача»), тогда как user-spec в прозе описывает состояние пункта как «будет заведён при сохранении». Оставлен текст из таблицы задачи: в user-spec это описание состояния, а не цитата текста подсказки, и фраза грамматически верна относительно создаваемой задачи. Если нужна буквальная формулировка user-spec — поменять одну строку в `src/i18n/ru.ts`.
**Tech debt:** Нет.
**Verification:** `npx tsc --noEmit` → clean; grep `followUps.` → по 10 ключей в `types.ts`, `en.ts`, `ru.ts`; `whyPrefix` оканчивается одним пробелом, `noticeCreated` содержит `{group}` и `{count}` в обоих языках.

## Task 02: Data model, migration v9 and sanitization

**Commit:** 29c04f1, 95a82ca (review fix)
**Summary:** Добавлены тип `FollowUp` и поле `Task.followUps`, версия данных поднята до 9: блок `version < 9` даёт каждой задаче пустой список. `sanitizeFollowUps` работает по Decision 11 на каждой загрузке, вне версионных веток. Повреждённый `tasks` или отдельная запись задачи, которая не является объектом, пропускается без исключения. Форма при сохранении переносит копию списка. Ревью: [dev-code-reviewer](0012-feat-follow-up-tasks-task-02-dev-code-reviewer-review.json) — approved_with_suggestions, 3 minor; [round 2](0012-feat-follow-up-tasks-task-02-dev-code-reviewer-review-round2.json) — approved; [dev-security-auditor](0012-feat-follow-up-tasks-task-02-dev-security-auditor-review.json) — approved, находок нет.
**Deviations:**
- **Хвост текста после обрезки.** После обрезки до 200 символов хвостовые пробелы и одиночные старшие суррогаты снимаются вместе (`/[\s\uD800-\uDBFF]+$/`). Правило `sanitizeGroupTitle` снимает только суррогат. Причина: инвариант «text trimmed» из Data Models. Иначе обрезка сразу после пробела оставляла хвост, и вторая загрузка снова переписывала текст. Первая версия с `trimEnd()` могла вернуть в конец спрятанный суррогат — исправлено по ревью, round 1.
- **Трактовки, которых нет в спеке:**
  - лимит 20 считает только валидные пункты;
  - запись задачи-массив пропускается так же, как не-объект;
  - `tasks`-массив считается «не объектом» и остаётся нетронутым.
- **Фикстура `makeV9Data`.** Тесты «already-migrated» для `groupOrder`/`title` переведены на новую полностью валидную фикстуру `makeV9Data`. Входные данные v8 теперь проходят блок v9, и цель этих тестов потерялась бы.
- **Лимиты продублированы.** 200 и 20 объявлены локальными константами в `migration.ts`, по образцу `GROUP_TITLE_MAX_LENGTH`. Task 03 заводит общие `FOLLOW_UP_TEXT_MAX_LENGTH`/`MAX_FOLLOW_UPS` в `src/logic/followUps.ts`, а `migration.ts` в её списке файлов только для чтения. Решение для лида: импортировать константы из `followUps.ts` в `migration.ts` (цикла импортов нет) или оставить дубль намеренно.
**Tech debt:** Дубль лимитов 200/20 до решения по пункту выше. Длина `id` и `createdTaskId` при загрузке не ограничивается — так же, как у остальных строковых полей задачи. Совет аудитора для Task 03/04: если появится поиск `data.tasks[item.createdTaskId]`, проверять `Object.hasOwn`.
**Verification:**
- `npx vitest run tests/unit/migration.test.ts`: новые тесты сначала падали (25 красных), затем 52/52;
- `npx vitest run`: 127/127;
- `npx tsc --noEmit`: clean;
- `npm run build`: OK;
- цикл санитизации `followUps` стоит после всех блоков `if (version < N)`.

## Task 03: Follow-up logic module

**Commit:** 002c5a7, 3fd6260 (review fix)
**Summary:** Создан чистый модуль `src/logic/followUps.ts` с API из таблицы задачи. `spawnFollowUps` и `revertSpawnedFollowUps` меняют переданный `PluginData` на месте, как `applyStatusTransition` и `dataStore`. Уведомление собирается за один проход с функцией-заменителем. Лимиты 200/20 экспортированы из `migration.ts` и реэкспортированы модулем логики — по заметке лида, дубля больше нет. Ревью: [dev-code-reviewer](0012-feat-follow-up-tasks-task-03-dev-code-reviewer-review.json) — approved_with_suggestions, 2 minor, оба исправлены тестами; [dev-security-auditor](0012-feat-follow-up-tasks-task-03-dev-security-auditor-review.json) — approved, находок нет.
**Deviations:**
- **Трактовки, которых нет в спеке:**
  - обрезка «Зачем» до 10 000 снимает висящий старший суррогат, как `sanitizeGroupTitle`;
  - `finalizeFollowUpDrafts` нормализует текст той же цепочкой, что `sanitizeFollowUps` (trim → 200 → снять хвостовые пробелы и суррогат). Иначе сохранённый текст менялся бы при следующей загрузке. Тест проверяет, что санитайзер оставляет результат без изменений;
  - пустой черновик не занимает место в лимите 20;
  - `revertSpawnedFollowUps` при неизвестной доске всё равно удаляет задачи из `tasks` и возвращает пункты в «не заведено»: шаги независимы.
- **Ревью:** тесты с `'constructor'` проходили и без проверки `hasOwnProperty`. По ревью добавлены тесты с задачей в прототипе `tasks`; без проверки они падают (проверено временной заменой на `in`).
- **Tech-spec:** чекбокса задачи в tech-spec нет — как и у Task 02, отмечены только чекбоксы в файле задачи.
- **Импорт словарей в тесте:** `en`/`ru` импортируются из `src/i18n/en` и `src/i18n/ru` напрямую, а не через `src/i18n/index.ts`. Индекс тянет svelte-сторы и `window.moment`, а для проверки шаблонов нужны только сами словари.
**Tech debt:** Цепочка нормализации текста пункта продублирована в `migration.ts` и `followUps.ts`; совпадение держит тест. Общую функцию можно вынести в `migration.ts`, когда правка этого файла будет в скоупе. Совет аудитора для Task 07: текст уведомления выводить как обычную строку, не как HTML.
**Verification:**
- `npx vitest run tests/unit/followUps.test.ts`: до модуля — падение импорта, затем 34/34;
- `npx vitest run`: 161/161;
- `npx tsc --noEmit`: clean;
- grep импортов stores/i18n/obsidian в `src/logic/followUps.ts`: нет совпадений.

## Task 05: Form editor

**Commit:** c2f0d43, 1c0735d (review fix)
**Summary:** Новый компонент `FollowUpsEditor.svelte` добавлен в форму между «Зачем» и строкой «Кто / Когда». Черновики строятся из копии `task.followUps`, поэтому форма не меняет объект задачи в сторе до Save. На Save черновики проходят через `finalizeFollowUpDrafts`, результат уходит в `onSave(task, spawnItemIds)`. Для группы `completed` блок только для чтения: Save оставляет исходный список без изменений и без id на заведение. `TaskModal` передаёт оба аргумента; однопараметровые колбэки `BoardLayout` компилируются без изменений. Ревью: [dev-code-reviewer](0012-feat-follow-up-tasks-task-05-dev-code-reviewer-review.json) — approved_with_suggestions, 2 minor (a11y); [round 2](0012-feat-follow-up-tasks-task-05-dev-code-reviewer-review-round2.json) — approved; [dev-security-auditor](0012-feat-follow-up-tasks-task-05-dev-security-auditor-review.json) — approved, 1 minor (`__proto__` в карте полей). Все три замечания исправлены.
**Deviations:**
- **Трактовки, которых нет в спеке:**
  - заголовок «После выполнения» рисует сам редактор, чтобы в режиме «только чтение» при пустом списке пропадал весь блок;
  - подсказка `followUps.toBacklogHint` выведена через `title` кнопки «→ в бэклог», видимого текста под строкой нет;
  - Enter при вводе через IME (`isComposing`) не вставляет строку;
  - у поля пункта `aria-label` = текст placeholder (по ревью).
- **CSS.** Obsidian задаёт фон кнопок селектором `button:not(.clickable-icon)` со специфичностью (0,1,1), проверено по `obsidian.asar`. Одиночный BEM-класс ему проигрывает. Цветовые правила кнопок редактора поэтому записаны как `.tm-follow-ups .tm-follow-ups__…`, без `!important`.
- **Escape.** Мок-модалка стенда пока не закрывается по Escape (это Task 07). Поэтому проверено, что стор не меняется до Save; сама форма при закрытии уничтожается в `TaskModal.onClose`.
**Tech debt:** `delete inputs[id]` в `remove()` фактически ничего не делает: при удалении строки Svelte сам записывает `null` в `bind:this`. В карте остаётся пустая запись на каждую удалённую строку, пока форма открыта. Это безвредно.
**Verification:**
- `npm run build`: OK, новых предупреждений нет;
- `npx tsc --noEmit`: clean;
- `npx playwright test tests/e2e/core.spec.ts`: 35/35 до и после ревью;
- временный спек-зонд (4 сценария, не закоммичен, удалён): место блока в форме; добавление, Enter-вставка и фокус; пометка и снятие; пустой помеченный пункт отброшен; лимиты 200 и 20; строка «заведено» без поля и кнопки; стор не меняется до Save; «только чтение» и пустой список в `completed`; текст с разметкой выводится буквально. Итог: 39/39 вместе с core.

## Task 04: Store integration

**Commit:** 7ca548a, 07a9122 (review fix)
**Summary:** `quickCompleteTask` и `moveTask` порождают незаведённые пункты в бэклог той же доски и возвращают их ids. `moveTask` делает это только при `to === 'completed' && from !== 'completed'`. `undoQuickComplete` получил хвостовой параметр `spawnedTaskIds = []` и откатывает порождение в том же `update`. Добавлен `createFollowUpTasks(boardId, parentTaskId, itemIds)`. Стор остаётся тонкой обёрткой над `src/logic/followUps.ts`: в каждой операции один `update` и один `persist()`, импорта `obsidian` нет. Ревью: [dev-code-reviewer](0012-feat-follow-up-tasks-task-04-dev-code-reviewer-review.json) — approved_with_suggestions, 2 minor по тестам, оба исправлены.
**Deviations:**
- **`today` в `quickCompleteTask`** вычисляется один раз вне колбэка и идёт и в `completedAt` родителя, и в `createdAt` порождённых задач. Раньше `formatDate(new Date())` вызывался внутри колбэка; значение то же.
- **`undoQuickComplete` при неизвестной доске** по-прежнему ничего не делает, и откат порождения тоже не выполняется: сохранено существующее поведение no-op.
- **Тесты сверх якорей:**
  - `createFollowUpTasks` сохраняет данные через `persist()`;
  - по ревью: префикс «Зачем» под `ru`;
  - по ревью: порождение идёт в доску из аргумента `boardId`, а не в активную.
- **Второй раунд ревью не запускался:** правки по ревью касаются только тестов и сделаны ровно так, как предложил ревьюер.
- **Tech-spec:** чекбокса задачи в tech-spec нет, отмечены только чекбоксы в файле задачи.
**Tech debt:** Промежуточное состояние до Task 07. `BoardLayout` вызывает Undo без `spawnedTaskIds`, поэтому в сборке между задачами 04 и 07 после ☑ → «Отменить» порождённые задачи остаются в бэклоге, а их пункты — «заведёнными». Так и заложено планом; закрывается проводкой в Task 07.
**Verification:**
- новые тесты сначала падали: 11 красных, импорт `createFollowUpTasks` отсутствовал;
- `npx vitest run tests/unit/dataStore.test.ts tests/unit/followUps.test.ts`: 61/61, после правок по ревью — 63/63;
- `npm test`: 175/175;
- `npx tsc --noEmit`: clean;
- `npm run build`: exit 0 (два существующих A11y-предупреждения в попапах);
- в `src/stores/` из `obsidian` импортируется только тип в `pluginStore.ts`, он существовал и раньше.

## Task 06: Card marker

**Commit:** 5c3572d, a430587 (review round 1), 735446a (review round 2)
**Summary:** `TaskCard.svelte` берёт незаведённые пункты через `pendingFollowUps`. При N > 0 выводится обычный `<span>` без обработчиков и `tabindex`: `↪ N` в верхней строке, `↪N` в компактном режиме. Подсказка `title` — тексты пунктов через `\n` (Decision 14). В обычном режиме маркер забирает у дедлайна `margin-left: auto`: пара стоит справа вместе, а без дедлайна маркер встаёт на его место. Отступ под ☑/✕ сохранён. Компактный маркер фиксированной ширины 1.75rem с `flex-shrink: 0`. Ревью: [dev-code-reviewer](0012-feat-follow-up-tasks-task-06-dev-code-reviewer-review.json) — approved_with_suggestions, 1 minor; [round 2](0012-feat-follow-up-tasks-task-06-dev-code-reviewer-review-round2.json) — approved_with_suggestions, 1 minor. Оба замечания исправлены.
**Deviations:**
- **Сжатие даты рядом с маркером (ревью, round 1).** В обычном режиме при 4 колонках маркер выталкивал дату под ☑/✕. Замер при окне 800px: правый край даты заходил на 12.3px за левый край ☑. Карточка без маркера помещалась. Когда маркер стоит перед датой, дате разрешено сжиматься с многоточием (`.tm-task-card__follow-ups + .tm-task-card__deadline`). После правки дата кончается за 12px до ☑, как у карточки без маркера. Карточки без маркера не затронуты. Этого правила нет в спеке.
- **`title` у даты (ревью, round 2).** Обрезанную дату иначе не прочитать. Span даты в обычном режиме получил `title={task.deadline}` — так же, как другие обрезаемые тексты карточки. Это правка существующего элемента за пределами спеки.
- **Ширина компактного маркера** 1.75rem выбрана под самое длинное значение `↪20`; в спеке ширина не задана.
- **Второй раунд ревью по правке round 2 не запускался:** добавлен один атрибут, ровно как предложил ревьюер.
- **Tech-spec:** чекбокса задачи в tech-spec нет, отмечены только чекбоксы в файле задачи.
**Tech debt:**
- В компактном режиме при 4 колонках маркер (1.75rem + gap) отнимает ширину у текста задачи. Это принято в user-spec (раздел «Риски»). Я это не замерял, оставлено на ручную проверку.
- E2E-тесты отрисовки маркера не закоммичены — они в плане Task 08.
**Verification:**
- `npm run build`: OK, только два известных A11y-предупреждения в попапах;
- `npx tsc --noEmit`: clean;
- `npx playwright test tests/e2e/core.spec.ts tests/e2e/0008-card-columns.spec.ts`: 51/51 после каждой правки;
- временный спек-зонд (не закоммичен, удалён), проверено:
  - текст маркера и подсказка в обоих режимах, с датой и без;
  - маркер стоит вплотную слева от даты, а без даты — на её месте;
  - при N = 0 маркера нет, `tabindex` у маркера нет;
  - клик по маркеру открывает форму, Enter на карточке открывает форму;
  - перетаскивание мышью, начатое на маркере, переносит карточку в другую группу;
  - замер наложения на ☑ при 4 колонках до и после правки;
- скриншоты обоих режимов и узкой карточки просмотрены.

## Task 07: UI wiring and notice

**Commit:** 6908919
**Summary:** Новый `src/ui/followUpNotice.ts` показывает одно уведомление Obsidian на действие: текущее название бэклога, суффикс, если бэклог скрыт; при `count === 0` или удалённой доске ничего не делает. ☑ кладёт `spawnedTaskIds` в тост завершения, Undo передаёт их в `undoQuickComplete` и снимает тосты удаления и завершения порождённых задач (Decision 10). Сохранение формы (создание и редактирование) порождает помеченные пункты через `createFollowUpTasks`. Перетаскивание и `__test.moveTask` идут через одну функцию `moveTaskAndNotify` из `useSortable.ts`. Стенд: мок `Notice` (`.notice` внутри `.notice-container`, `textContent`, автоудаление через 5 с), мок `Modal` закрывается по Escape, `resetData` убирает оставшиеся уведомления. Ревью: [dev-code-reviewer](0012-feat-follow-up-tasks-task-07-dev-code-reviewer-review.json) — approved_with_suggestions, 2 minor.
**Deviations:**
- **Порядок ревью.** Ревью шло по незакоммиченному диффу, пока Playwright был занят Task 06. Принятое замечание (комментарий о допущении «одна модалка» в моке) вошло в основной коммит, отдельного коммита `fix: address review` нет.
- **Отклонено/передано лиду:** замечание ревьюера про `src/stores/dataStore.ts:127` — у `undoQuickComplete` осталось значение по умолчанию `spawnedTaskIds = []` и устаревший комментарий «пока вызывающий код не передаёт id». Теперь единственный вызов их передаёт. Файл в этой задаче только для чтения, поэтому не менял. Предложение: сделать параметр обязательным и убрать комментарий (2 строки).
- **Трактовки, которых нет в спеке:**
  - длительность мока `Notice` по умолчанию — 5000 мс (у реального API значение не задокументировано в `obsidian.d.ts`);
  - мок `Notice` принимает и `DocumentFragment` (через `appendChild`), как настоящий API; фича передаёт только строку;
  - Escape в моке закрывает все открытые мок-модалки, а не только верхнюю, — сейчас модалка всегда одна, допущение записано комментарием.
- **AC «нет импорта `obsidian` в `src/stores/`»:** grep находит `import type { Plugin }` в `pluginStore.ts` — он существовал до фичи, стирается при сборке и в рантайме ничего не импортирует.
**Tech debt:** Если в течение 7 секунд порождённую задачу завершить ☑ и у неё самой есть пункты, её пункты тоже породят задачи; Undo родителя удалит порождённую задачу и снимет её тост, а «внуки» останутся обычными задачами в бэклоге. Сценарий вне спеки, не исправлялся.
**Verification:**
- `npm run build`: exit 0 (два существующих A11y-предупреждения в попапах);
- `npx tsc --noEmit`: clean;
- `npm test`: 175/175;
- `npx playwright test`: 102/102 (весь существующий набор, после коммитов Task 06);
- временный спек-зонд (6 сценариев, не закоммичен, удалён) — 6/6: ☑ с пунктами даёт одно уведомление `Tasks created in "Backlog": 2`, без пунктов — нет; `__test.moveTask` в completed — уведомление, перестановка внутри completed — нет и ничего не порождает; **настоящий drag Sortable** (Playwright `dragTo`) в completed — уведомление; Save формы с пометкой (редактирование и создание) порождает и уведомляет, Escape — ничего не порождает; Undo удаляет порождённые задачи, возвращает пункты в «не заведено», снимает тосты удаления и завершения порождённых задач и не трогает чужой тост удаления; скрытый и переименованный в `$& {count} <b>x</b>` бэклог — текст буквально с суффиксом `(group hidden)`, без элемента `<b>`.

## Task 08: Feature E2E scenarios

**Commit:** d80e07f, a31a989 (review fix)
**Summary:** Новый файл `tests/e2e/0012-follow-up-tasks.spec.ts` — 21 сценарий, у каждого из AC-1…AC-12 есть хотя бы один. AC-13 не покрыт намеренно: стенд закрепляет локаль `en`. Задачи для сценариев готовит фикстура `seed`: снапшот v9 из живого стора проходит через `resetData` → `migrateData`, то есть тот же путь, что и реальная загрузка. Проверки идут по данным стора, тексту уведомлений и видимым элементам; классы служат только локаторами. Ревью: [dev-code-reviewer](0012-feat-follow-up-tasks-task-08-dev-code-reviewer-review.json) — approved_with_suggestions, 4 minor, все приняты; [round 2](0012-feat-follow-up-tasks-task-08-dev-code-reviewer-review-round2.json) — approved.
**Deviations:**
- **Хелпер перетаскивания перенесён (по ревью).** `dragCardToGroup` с двумя константами дословно перенесён из `0011-group-customization.spec.ts` в `tests/e2e/helpers.ts`, 0011 теперь его импортирует. Файл 0011 не входит в список файлов задачи. Альтернатива — дубль хелпера на 60 строк.
- **AC-5 — настоящее перетаскивание мышью.** В тексте задачи вход в completed проверяется через `moveTask`. По ревью в AC-5 он заменён на жест мышью через SortableJS и обработчик `onEnd`. Программный путь `__test.moveTask` в completed остаётся покрыт в AC-8 и AC-10 (переименованный бэклог).
- **Сценарии сверх списка задачи:**
  - ☑ на задаче без незаведённых пунктов — ничего не создаётся, уведомления нет;
  - в первом сценарии AC-7 порождённая задача за 7 секунд перемещается в другую группу, Undo её всё равно удаляет;
  - по ревью: Undo при уже удалённой порождённой задаче снимает её тост удаления (Decision 10).
- **Хелперы.** В `helpers.ts` только экспорт `groupAddButton` и перенесённый `dragCardToGroup`. Остальные хелперы (`seed`, открыть форму, добавить и пометить пункт, уведомления) локальны в спеке, потому что другие файлы их не используют.
- **Якорь TDD.** Сборки без фичи нет, поэтому падение сценариев без фичи доказано мутационной проверкой (см. Verification).
- **Tech-spec:** чекбокса задачи в tech-spec нет, отмечены только чекбоксы в файле задачи.
**Tech debt:**
- `docs/testing/infrastructure.md` не обновлён: счётчики тестов, таблица spec-файлов, `dragCardToGroup` в таблице хелперов. Файл вне скоупа задачи — на `/done`.
- Не проверяется, что Undo останавливает таймеры снятых тостов (наблюдение ревьюера round 2). Сработавший таймер попытался бы удалить уже удалённую задачу, вреда нет.
**Verification:**
- baseline: `npx playwright test` — 102/102 до изменений;
- мутационная проверка, раунд 1 — временно сломаны шесть строк продукта:
  - `moveTask` порождает и при перестановке внутри completed;
  - Undo без `spawnedTaskIds`;
  - нет суффикса скрытой группы в уведомлении;
  - нет `maxlength` у поля пункта;
  - форма всегда редактируемая;
  - Save редактирования не заводит помеченные пункты.
  Упали ровно шесть целевых сценариев (AC-5, AC-7, AC-10 hidden, AC-2, AC-12, AC-9 edit), остальные 14 прошли;
- мутационная проверка, раунд 2: без `dismissed.add` для порождённых задач в `handleUndo` упал только новый сценарий Decision 10. Код после обеих проверок восстановлен;
- `npx playwright test tests/e2e/0012-follow-up-tasks.spec.ts`: 20/20; после ревью 0012 + 0011 — 39/39;
- `npx playwright test`: 123/123.

## Task 09: Pre-deploy QA

**Commit:** см. `chore: complete task 9` (кода не менялось, фикс-коммитов нет)
**Summary:** Приёмочная проверка фичи. Все автоматические проверки зелёные. Каждый из AC-1…AC-13 user-spec и каждый критерий tech-spec сопоставлен с конкретным тестом или проверкой. Отчёт: `logs/working/qa-report.json` (в `.gitignore`, не коммитится), `status: passed`, находок нет. AC-13 доказан типизацией словарей `Record<TranslationKey, string>`, unit-тестом текста уведомления на обоих словарях и отсутствием захардкоженного видимого текста в `FollowUpsEditor.svelte`. Русский интерфейс e2e не проверяется: стенд закрепляет `en`.
**Deviations:**
- Фаза пересборки Docker из `pre-deploy-qa` пропущена: окружения и деплоя у плагина нет (по указанию лида).
- Старый `logs/working/qa-report.json` от фичи 0011 перезаписан.
- Отложены на пользователя (`deferredToPostDeploy`), это ручные проверки из user-spec: темы в живом Obsidian, вид маркера в компактном и 4-колоночном режимах, настоящее уведомление Obsidian.
**Tech debt:** Новых нет. Замечание Task 07 про значение по умолчанию `spawnedTaskIds = []` в `undoQuickComplete` уже закрыто: параметр обязательный. Устаревшие счётчики в `docs/testing/infrastructure.md` — скоуп Task 10.
**Verification:**
- `npx tsc --noEmit`: clean;
- `npm run build`: exit 0, только два существующих A11y-предупреждения в попапах;
- `npm run test:all`: vitest 175/175 (8 файлов), Playwright 123/123 — весь набор, включая 0012 (21);
- `grep -rn "from 'obsidian'" src/stores src/logic`: только существующий `import type { Plugin }` в `pluginStore.ts`, в рантайме не импортируется.

## Task 10: Documentation update

**Commit:** 5a766c4, 84c9827 (review fix)
**Summary:** Документация приведена к тому, что вошло в фичу. В `docs/technical.md` обновлены: пример data.json (версия 9, `followUps`), интерфейсы `FollowUp` и `Task`, дерево компонентов, `onSave(task, spawnItemIds)`, отмена ☑ со `spawnedTaskIds`, миграция v9 и санация `followUps`, компактный режим, лимиты, структура проекта и таблица решений. Добавлен раздел «Пост-задачи»: триггеры, поля заведённой задачи, уведомление, форма и режим «только чтение». Кроме того: продуктовое описание в `overview.md`, счётчики и таблицы в `infrastructure.md` (175 unit / 123 e2e), раздел 21 ручных сценариев, записи в CHANGELOG и абзацы в README на обоих языках. Источник правды — код, а не tech-spec. Например, у `undoQuickComplete` параметр `spawnedTaskIds` описан как обязательный. Ревью: [dev-code-reviewer](0012-feat-follow-up-tasks-task-10-dev-code-reviewer-review.json) — approved_with_suggestions, 4 minor, все исправлены.
**Deviations:**
- **Скилл `documentation-writing`.** Скилл рассчитан на `.claude/skills/project-knowledge/`, которого в репозитории нет. Его принципы применены к файлам из задачи. Новые сведения записаны прозой и таблицами без новых блоков кода. Существующие блоки кода (data.json, интерфейсы, набросок `migrateData`) дополнены, потому что так уже устроен `technical.md`.
- **Раздела про ☑ в `test-scenarios.md` не было.** Задача просит пометку «в существующих разделах ☑ и перетаскивания», но существует только раздел перетаскивания. Пометка добавлена в 6.4, а ☑ описан в новом разделе 21 (21.4, 21.5).
- **Сверх списка задачи** — только то, что без правки стало бы неверным:
  - в `technical.md`: поле формы в «Создании задачи», маркер в «Компактном режиме» (ширины и поведение даты рядом с маркером), строки в таблице «Ключевые решения»;
  - в `infrastructure.md`: хелперы `dragCardToGroup` и `groupAddButton`, абзац про настоящее перетаскивание мышью вместо устаревшего «SortableJS не поддерживает программный drag-and-drop», очистка уведомлений в `resetData`;
  - в `overview.md`: строка про маркер в описании карточки.
- **Время прогона unit-тестов.** Цифра «~300 мс» заменена на «~0,5 с на сами тесты» — по замеру (533 мс).
- **Tech-spec:** чекбокса задачи в tech-spec нет, отмечены только чекбоксы в файле задачи.
**Tech debt:** Раздел «Требуемая версия Node» в `infrastructure.md` по-прежнему описывает snap-путь, а сейчас нужная версия Node стоит через nvm (`~/.nvm/versions/node/v22.23.2`). Раздел вне скоупа фичи, не менялся — вопрос для TD-06 или `/done`.
**Verification:**
- `grep -n "followUps" docs/technical.md`: поле описано в примере data.json, в интерфейсе `Task`, в миграции, в санации и в разделе «Пост-задачи»;
- в обоих CHANGELOG есть «version 9» / «до 9»;
- `npx vitest run`: 175/175 в 8 файлах;
- `npx playwright test --list`: 123 теста в 6 файлах, в 0012 — 21. Ревьюер перепроверил оба счётчика запуском;
- `git diff --stat` затрагивает только файлы задачи.
