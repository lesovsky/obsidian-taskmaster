---
status: planned                    # planned -> in_progress -> done
depends_on: ["09"]                 # ID задач-зависимостей (строки: ["01", "02"])
wave: 7                            # волна параллельного выполнения
skills: [code-writing]             # МАССИВ скиллов для загрузки
verify: bash — `npx playwright test tests/e2e/0011-group-customization.spec.ts`
reviewers: [dev-test-reviewer, dev-code-reviewer, dev-security-auditor]  # явно указать. Пусто = fallback на defaults
teammate_name:                     # имя агента-исполнителя (опционально; если не задано — генерируется по описанию задачи)
---

# Task 10: Feature E2E scenarios

## Required Skills

Перед выполнением задачи загрузи:
- `/skill:code-writing` — [skills/code-writing/SKILL.md](~/.claude/skills/code-writing/SKILL.md)

## Description

Это приёмка фичи 0011 автотестами. К моменту старта задачи весь продукт уже собран: модель
данных и миграция (Task 3), чистая логика перестановки и разрешения названия (Tasks 4–5),
отображение названий (Task 6), доска в заданном порядке с атрибутом `data-group-container`
(Task 7), попап настроек со строками `data-settings-group`, полем названия и стрелками
(Task 8), а существующие E2E уже переведены с поиска по названиям групп на идентификаторы
(Task 9). Новый файл `tests/e2e/0011-group-customization.spec.ts` покрывает поведение фичи
целиком — по списку из tech-spec, раздел Testing Strategy / E2E tests.

Два обстоятельства делают эту задачу не рутинной, и их нужно держать в голове от первой
строки:

**Первое — сценарий перетаскивания несёт основной вес.** Главный риск фичи в том, что смена
порядка групп способна пересоздать DOM-элементы, к которым привязана SortableJS, и
перетаскивание отвалится. Выбранный подход (CSS `order`, Decision 1) должен этот риск снять,
но единственное доказательство — тест, который **в той же сессии, без перезагрузки** меняет
порядок и тут же тащит задачу в обе стороны, обязательно с участием сворачиваемой группы
(`backlog` / `completed`) — у них привязка устроена иначе и ломается вероятнее. User-spec
прямо предупреждает: проверка вида «задать порядок → перезагрузить → перетащить» покажет
зелёный результат при сломанном продукте, потому что после перезагрузки библиотека
инициализируется заново на уже правильном порядке. Ни одного `page.goto()`, ни одного
`resetData()` между сменой порядка и перетаскиванием быть не должно.

**Второе — проверять порядок по индексу элемента бессмысленно.** По Decision 12 порядок
задаётся CSS-свойством `order` на существующих обёртках, DOM-последовательность блоков в
`BoardLayout.svelte` при этом не меняется никогда. Значит `locator.nth(0)` и любые проверки
«кто раньше в DOM» на доске пройдут одинаково и до, и после настройки — то есть не докажут
ничего. Порядок на доске проверяется вычисленным значением `order` и/или экранной геометрией.
Важная оговорка: это относится **только к доске**. В попапе настроек строки рендерятся
циклом по сконфигурированному порядку, там DOM-последовательность настоящая, и проверять
порядок строк по их последовательности в DOM — правильно.

**Третье — контракт поля ввода названия.** По Decision 4 (абзац «The editing field is not a
display site») поле в попапе показывает **сырое сохранённое название**: пустое, если группу
никогда не переименовывали, и дефолтное локализованное название — как `placeholder`, а не как
`value`. Поле не проходит через хелпер разрешения названия. Из этого следует наблюдаемое
свойство, которое надо проверить отдельно: открыть попап и нажать «Сохранить», ничего не
трогая, не должно превратить дефолты в явные названия всех шести групп. Сценарии Сц.2 и Сц.14
построены именно на этом контракте — если поле начнёт подставлять разрешённое название,
они обязаны покраснеть.

Адресация групп в новых сценариях — только по идентификаторам: `[data-group-container="…"]`
на доске, `[data-settings-group="…"]` в попапе. Поиск по отображаемому названию запрещён:
в этих сценариях названия ровно то, что меняется.

## What to do

1. Изучить `tests/e2e/helpers.ts` **в том виде, в каком его оставила Task 9** — не в том, что
   описан ниже по памяти. Использовать существующие хелперы (`standardBeforeEach`,
   `createTask`, `openBoardSettings`, `saveBoardSettings`, `closeBoardSettings`,
   `openGroupSettings`, `expandGroup`) вместо того, чтобы писать свои дубли.

2. Завести локальные хелперы нового файла (в самом spec-файле, не в `helpers.ts` — общими они
   ни для кого не станут):
   - получение вычисленного значения `order` обёртки группы по `data-group-container`;
   - получение ранга группы: отсортировать видимые группы по паре (`order`, а при равенстве —
     позиция в DOM) и вернуть индекс; так удобно писать утверждения «focus идёт раньше
     inProgress», не завязываясь на конкретные числа;
   - получение экранной геометрии обёртки (`boundingBox`) для проверки «выше/левее»;
   - установка названия группы в открытом попапе (по `data-settings-group`);
   - нажатие стрелки вверх/вниз в строке группы;
   - **настоящее перетаскивание карточки мышью** (см. п. 5);
   - снятие снимка сохранённых данных из `localStorage` и перезапуск приложения на нём
     (симуляция перезагрузки, см. п. 4).

3. Написать сценарии. Именование — как в соседних наборах: `Сц.N краткое описание`.

   - **Сц.1 переименование группы видно везде и переживает перезагрузку.** Задать группе
     новое название, сохранить. Проверить: заголовок группы на доске, заголовок свёрнутой
     группы (взять `backlog` или `completed` — у них заголовок отдельный), заголовок попапа
     настроек именно этой группы. Затем симулировать перезагрузку (п. 4) и убедиться, что
     название на месте.
   - **Сц.2 очистка названия возвращает дефолт.** Сценарий целиком про контракт поля из
     Decision 4, поэтому проверяются все три его состояния подряд, в одном тесте:
     1. *Группу не переименовывали.* Открыть попап — у строки группы поле **пустое**
        (`value` — пустая строка), а дефолтное локализованное название показано атрибутом
        `placeholder`. Это исходное состояние; проверять его обязательно, иначе сценарий не
        отличит правильную реализацию от той, что подставляет разрешённое название в поле.
     2. *Группу переименовали.* Задать название, сохранить, открыть попап заново — поле
        показывает сохранённое название в `value`.
     3. *Название очистили.* Очистить поле, сохранить. На доске — дефолтное локализованное
        название. Открыть попап — поле снова пустое, дефолт снова только в `placeholder`.

     Ни на одном шаге не проверяй дефолт через `value`: совпадение `value` с дефолтом здесь
     означает ровно сломанный контракт. Смежная проверка «сохранение без правки названий не
     замораживает дефолты» вынесена в отдельный сценарий Сц.14.
   - **Сц.3 стрелки меняют порядок групп на доске.** Переставить группу стрелкой, сохранить,
     проверить новый порядок по `order` и по геометрии. В этом же сценарии — что секция
     заметок (`.tm-board-layout__notes`) осталась ниже всех групп (Decision 2).
   - **Сц.4 видимая группа перепрыгивает скрытую с первого нажатия.** Скрыть группу, стоящую
     между двумя видимыми, сохранить. Открыть попап, нажать стрелку у видимой группы **один
     раз**, сохранить — порядок двух видимых на доске поменялся местами. Именно «с первого
     нажатия»: второе нажатие не делать.
   - **Сц.5 перетаскивание работает сразу после смены порядка, без перезагрузки** — ключевой
     сценарий, требования в п. 5.
   - **Сц.6 повторное открытие попапа сохраняет порядок строк.** Задать порядок, в котором
     скрытая группа стоит не в конце, сохранить, закрыть, открыть попап заново. Собрать
     последовательность значений `data-settings-group` и сверить с ожидаемой: скрытая строка
     стоит на своей позиции, а не съехала в конец списка.
   - **Сц.7 «Отмена» откатывает несохранённые название и порядок.** Изменить название и
     нажать стрелку, нажать «Отмена». На доске — прежние название и порядок; при повторном
     открытии попапа поле показывает сохранённое значение, а строки — сохранённый порядок.
   - **Сц.8 клик мимо попапа откатывает то же самое.** То же, но закрытие кликом по
     `.tm-popup-overlay` (как в `0006-group-visibility.spec.ts`, Сц.4).
   - **Сц.9 две доски держат независимые названия и порядок.** На доске A задать своё
     название и порядок, создать доску B, убедиться, что у неё дефолтные название и порядок,
     задать ей другие, вернуться на A и убедиться, что настройки A на месте и не протекли.
   - **Сц.10 название из 40 символов не ломает строку заголовка.** Снять высоту заголовка
     группы с дефолтным названием, задать 40 символов, сохранить, снять высоту снова —
     не изменилась (нет переноса на вторую строку); текст усечён (`scrollWidth` элемента
     заголовка больше его `clientWidth`), а ширина элемента заголовка не вылезает за ширину
     обёртки группы.
   - **Сц.11 переключение видимости меняет поведение стрелок до сохранения.** В одном
     открытии попапа: снять видимость с группы-соседки и **не сохраняя** нажать стрелку у
     видимой группы. Ожидание — стрелка сработала по новому, несохранённому состоянию, то
     есть перепрыгнула только что скрытую группу. Проверить по порядку строк в попапе (до
     сохранения доска ещё старая), затем сохранить и подтвердить результат на доске.
   - **Сц.12 попап с полным списком строк не выходит за экран, тело прокручивается.** Задать
     невысокий viewport, открыть попап, проверить: его `boundingBox` целиком внутри вьюпорта
     по вертикали, а у прокручиваемого контейнера `scrollHeight > clientHeight` и прокрутка
     реально происходит (после `scrollTop`-сдвига значение изменилось).
   - **Сц.13 подсказка в пустой группе зависит от того, переименована ли группа.** Критерий
     user-spec: «Пустая переименованная группа показывает нейтральную подсказку, а группа с
     дефолтным названием — прежнюю». Взять две пустые группы (после `standardBeforeEach` все
     шесть пусты): одну переименовать и сохранить, вторую не трогать. Проверить внутри
     соответствующих `[data-group-container="…"]`: у переименованной `.tm-empty-state`
     содержит нейтральный текст, у нетронутой — её собственную прежнюю подсказку. Затем
     очистить название первой группы, сохранить — её подсказка вернулась к прежней. Тексты
     не выдумывай и не хардкодь по памяти: возьми точные строки из `src/i18n/en.ts` (ключи
     `emptyState.*` и нейтральный ключ, добавленный Task 2 — его имя посмотри в файле).
     Для обеих групп бери обычные, не сворачиваемые (`focus`, `delegated`, `inProgress`,
     `orgIntentions`): у сворачиваемых `.tm-empty-state` живёт внутри тела, которого в
     свёрнутом состоянии нет.
   - **Сц.14 сохранение без правки названий не превращает дефолты в явные названия.** Прямое
     следствие Decision 4 и главный способ поймать регресс контракта поля. Открыть попап
     настроек доски и нажать «Сохранить», не трогая ни одного поля названия. Проверить:
     1. в сохранённых данных (`window.__test.getDataStore()`) у **всех шести** групп активной
        доски `title` — пустая строка;
     2. наблюдаемое следствие на доске: пустая группа по-прежнему показывает свою прежнюю
        подсказку, а не нейтральную (если бы сохранение записало дефолты как явные названия,
        все шесть групп считались бы переименованными и подсказки переключились бы);
     3. заголовки групп на доске — прежние дефолтные названия.

     Оговорка про язык: буквальную проверку «после такого сохранения смена языка по-прежнему
     переводит заголовки групп» в E2E выполнить нельзя — харнесс жёстко ставит `en`
     (`locale.set('en')` в `tests/harness/main.ts`), а `window.__test.updateSettings({language})`
     меняет только поле в сторе и не трогает стор `locale`, так что «переключение языка» в
     тесте было бы пустой операцией и не доказывало бы ничего. Расширять харнесс эта задача не имеет права
     (см. Implementation hints). Поэтому проверяется причина, а не следствие: пустой `title`
     в данных — это ровно то условие, при котором хелпер разрешения названия отдаёт
     локализованный дефолт, и перевод при смене языка гарантирован его unit-тестами (Task 5).
     Запиши эту оговорку в отчёт, а не прячь.

4. **Симуляция перезагрузки — только через сохранённые данные.** Прямой `page.goto('/')` для
   проверки сохранности не годится: харнесс на старте инициализируется из `migrateData(null)`
   и не читает `localStorage`, поэтому «перезагрузка» просто обнулит доску, и тест на
   сохранность станет ложно-зелёным либо ложно-красным. Правильный способ: прочитать снимок
   из `localStorage['tm-test-data']`, затем передать его в `window.__test.resetData(снимок)` —
   у снимка есть поле `version`, поэтому харнесс прогонит его через `migrateData` и
   перерисует доску из сохранённых данных. Это и есть настоящая проверка круга
   «сохранили → загрузили → отрисовали».

   Две особенности `resetData`, которые надо учесть явно:
   - **Снимок читается до вызова.** `resetData` первым делом делает
     `localStorage.removeItem('tm-test-data')`. Сначала прочитай снимок в переменную теста,
     потом вызывай.
   - **Активная доска сбрасывается на первую.** В конце `resetData` стоит
     `activeBoardId = data.boards[0].id`. Если в сценарии больше одной доски (Сц.9), после
     восстановления снимка ты окажешься на первой доске независимо от того, где был до этого.
     Не считай это багом продукта и не проверяй сохранность на «текущей» доске вслепую:
     после восстановления явно переключись на нужную через
     `page.selectOption('.tm-board-header__select', boardId)` и только потом утверждай.

5. **Сценарий перетаскивания (Сц.5) — требования, от которых нельзя отступать:**
   - Порядок шагов строго такой: создать задачи → сменить порядок групп через попап и
     сохранить → **сразу** перетащить. Никакой перезагрузки, никакого `resetData`, никакого
     `page.goto` между сменой порядка и перетаскиванием.
   - Перетаскивание — настоящим вводом мыши, через SortableJS. `window.__test.moveTask`
     (и хелпер `moveTask` из `helpers.ts`) в этом сценарии использовать **запрещено**: он
     дёргает стор напрямую, минуя библиотеку, и проверяет ровно то, что не сломано.
   - Перетащить **в обе стороны**: A → B и B → A.
   - Обязательно задействовать сворачиваемую группу, и это должен быть **`backlog`**, а не
     `completed`. В группе «Выполнено» кнопка добавления отключена (`onAdd={null}` в
     `BoardLayout.svelte`), задачу там не создать, а перенос задачи в неё дополнительно
     запускает смену статуса — лишний шум в сценарии, который проверяет совсем другое.
     Пара для перетаскивания: обычная группа (например `focus`) и `backlog`, развёрнутый.
     Важно: `backlog` свёрнут по умолчанию, поэтому создать в нём задачу до раскрытия нельзя —
     разворачивать нужно раньше, чем создавать в нём что-либо
     через `expandGroup` **до** первого перетаскивания.
   - Дополнительно — проверка сохранности DOM-узлов через метку на элементе (`page.evaluate`,
     свойство на самом узле). **Куда ставить метку:**
     - на обёртки групп `[data-group-container="…"]` — они есть в DOM всегда, независимо от
       свёрнутости, и именно про них Decision 1 утверждает, что смена порядка их не
       пересоздаёт;
     - на тела групп `[data-group-id="…"]` — только для тех групп, чьё тело реально в DOM в
       момент простановки метки. У `backlog` и `completed` тело живёт внутри
       `{#if !collapsed}` и в свёрнутом состоянии **отсутствует**, так что метку на тело
       `backlog` можно ставить только после `expandGroup`. Порядок шагов: развернуть
       `backlog` → проставить метки (обёртки + тела) → сменить порядок и сохранить →
       проверить, что метки на месте.

     Проверка меток подтверждает механизм из Decision 1, но **не заменяет** настоящее
     перетаскивание, а дополняет его.
   - Если настоящее перетаскивание в харнессе не заводится с первого подхода — разбираться
     (см. Implementation hints), а не подменять его на `moveTask`. Если после честных попыток
     оно принципиально невозможно — остановиться и доложить, а не сдать зелёный тест, который
     ничего не проверяет.

6. Прогнать `npx playwright test tests/e2e/0011-group-customization.spec.ts` — весь набор
   зелёный. Затем прогнать `npx playwright test` целиком и убедиться, что новый файл не
   развалил соседние наборы (общий `localStorage`, `workers: 1`, `fullyParallel: false`).

7. Провести негативный контроль каждого утверждения о порядке и перетаскивании (см. TDD
   Anchor) — без него ни один из этих сценариев не считается написанным.

## TDD Anchor

Тесты здесь — сам продукт задачи, поэтому классический цикл «красный до кода» неприменим:
код уже написан предыдущими волнами. Вместо него обязателен **негативный контроль**: каждое
утверждение должно быть доказано способным падать. Тест, который проходит в обоих
состояниях мира, — это не тест, а строчка в отчёте.

Порядок работы по каждому сценарию: написать → убедиться, что он зелёный на текущем
продукте → временно сломать условие → убедиться, что он покраснел → вернуть как было.

Правило без исключений: **у каждого сценария должно быть хотя бы одно возмущение, дающее
красный**. Возмущения с ожидаемым зелёным результатом (они есть у Сц.5) полезны — они
отделяют настоящую проверку от ложно-зелёной, — но сами по себе падаемость не доказывают и
не засчитываются за негативный контроль.

Обязательный минимум негативного контроля:

- `tests/e2e/0011-group-customization.spec.ts::Сц.3` — временно убрать сохранение
  (не нажимать «Сохранить» после стрелки) → утверждение о порядке на доске должно упасть.
  Если оно проходит и без сохранения, значит проверка смотрит на DOM-индекс, а не на `order`,
  и её надо переписать (Decision 12).
- `tests/e2e/0011-group-customization.spec.ts::Сц.4` — временно не нажимать стрелку вовсе →
  тест должен упасть.
- `tests/e2e/0011-group-customization.spec.ts::Сц.5` — три возмущения, и **первое из них
  обязательно**, потому что два других зелёные и сами по себе ничего не доказывают:
  - **(обязательное, ожидается красный)** временно убрать сам вызов перетаскивания, оставив
    всё остальное как есть → сценарий обязан **упасть**. Это единственное доказательство, что
    зелёный результат вызван перетаскиванием, а не тем, что задача и так лежала где надо.
    Если сценарий проходит без перетаскивания — он не тест, и его надо переписать: скорее
    всего, утверждение смотрит на состояние, которое не менялось, или ждёт не тот счётчик.
  - **(ожидается красный)** временно нацелить сброс на третью группу (не ту, что заявлена
    приёмником) → сценарий обязан упасть. Это доказывает, что утверждение проверяет
    конкретного приёмника, а не факт «карточка куда-то делась».
  - **(ожидается зелёный)** временно закомментировать шаг смены порядка → тест остаётся
    зелёным: перетаскивание работает и без перестановки.
  - **(ожидается зелёный)** временно вставить перезагрузку между перестановкой и
    перетаскиванием → тест тоже остаётся зелёным; это тот самый ложно-зелёный вариант, ради
    отличия от которого сценарий написан. Убедись, что в финальной версии перезагрузки нет.
- `tests/e2e/0011-group-customization.spec.ts::Сц.10` — временно задать название в 5 символов
  → проверка усечения (`scrollWidth > clientWidth`) должна упасть.
- `tests/e2e/0011-group-customization.spec.ts::Сц.13` — временно переименовать и контрольную
  группу тоже → утверждение «у группы с дефолтным названием прежняя подсказка» должно упасть.
- `tests/e2e/0011-group-customization.spec.ts::Сц.14` — временно вписать явное название одной
  группе перед «сохранением без правки» → утверждение «у всех шести групп `title` пустой»
  должно упасть.

Полный список сценариев, которые должны появиться в файле:

- `tests/e2e/0011-group-customization.spec.ts::Сц.1` — переименование видно в заголовке
  группы, в свёрнутом заголовке и в попапе настроек группы; переживает перезагрузку
- `tests/e2e/0011-group-customization.spec.ts::Сц.2` — поле названия показывает сырое
  сохранённое значение: пустое до переименования, название после, снова пустое после очистки;
  дефолт всегда только в `placeholder`; на доске после очистки — дефолт
- `tests/e2e/0011-group-customization.spec.ts::Сц.3` — стрелки меняют порядок групп на доске;
  заметки остаются ниже всех групп
- `tests/e2e/0011-group-customization.spec.ts::Сц.4` — видимая группа перепрыгивает скрытую,
  порядок меняется с первого нажатия
- `tests/e2e/0011-group-customization.spec.ts::Сц.5` — перетаскивание в обе стороны сразу
  после смены порядка, без перезагрузки, с участием сворачиваемой группы
- `tests/e2e/0011-group-customization.spec.ts::Сц.6` — повторное открытие попапа сохраняет
  порядок строк, скрытые на своих позициях
- `tests/e2e/0011-group-customization.spec.ts::Сц.7` — «Отмена» откатывает несохранённые
  название и порядок
- `tests/e2e/0011-group-customization.spec.ts::Сц.8` — клик мимо попапа откатывает то же
- `tests/e2e/0011-group-customization.spec.ts::Сц.9` — две доски держат независимые названия
  и порядок
- `tests/e2e/0011-group-customization.spec.ts::Сц.10` — название из 40 символов не ломает
  строку заголовка группы
- `tests/e2e/0011-group-customization.spec.ts::Сц.11` — переключение видимости меняет
  поведение стрелок до сохранения
- `tests/e2e/0011-group-customization.spec.ts::Сц.12` — попап при полном списке строк
  остаётся в пределах экрана, его тело прокручивается
- `tests/e2e/0011-group-customization.spec.ts::Сц.13` — пустая переименованная группа
  показывает нейтральную подсказку, пустая группа с дефолтным названием — прежнюю
- `tests/e2e/0011-group-customization.spec.ts::Сц.14` — сохранение настроек без правки
  названий оставляет `title` всех шести групп пустым и не переключает подсказки

## Acceptance Criteria

- [ ] Создан `tests/e2e/0011-group-customization.spec.ts` со всеми четырнадцатью сценариями
- [ ] `npx playwright test tests/e2e/0011-group-customization.spec.ts` — все тесты проходят
- [ ] `npx playwright test` целиком проходит: новый набор не сломал соседние
- [ ] Ни один сценарий не адресует группу или строку попапа по отображаемому названию —
      только `data-group-container` и `data-settings-group`
- [ ] Порядок групп на доске нигде не проверяется по индексу элемента в DOM: используются
      вычисленный `order` и/или экранная геометрия
- [ ] В Сц.5 перетаскивание выполнено настоящим вводом мыши; `moveTask` / `window.__test.moveTask`
      в этом сценарии не используются
- [ ] В Сц.5 между сменой порядка и перетаскиванием нет ни перезагрузки, ни `resetData`,
      ни `page.goto`
- [ ] В Сц.5 задача перенесена в обе стороны, и одна из пар включает `backlog`, развёрнутый
      через `expandGroup`
- [ ] Сц.5 доказанно способен упасть: с удалённым вызовом перетаскивания он красный
- [ ] Ни в одном сценарии дефолтное название не проверяется через `value` поля ввода —
      только через `placeholder` (Decision 4)
- [ ] Сц.14 подтверждает, что «сохранить, ничего не трогая» оставляет `title` всех шести
      групп пустым
- [ ] Сц.13 покрывает критерий user-spec про подсказку в пустой группе (нейтральная у
      переименованной, прежняя у группы с дефолтным названием)
- [ ] Проверка сохранности сделана через снимок `localStorage` + `resetData`, а не через
      `page.goto('/')`; в Сц.9 после восстановления снимка доска выбирается явно
- [ ] Проведён негативный контроль из TDD Anchor; временные поломки убраны из финального кода
- [ ] Новый файл не оставляет за собой состояния, влияющего на другие наборы (в `beforeEach`
      — `page.goto('/')` и `standardBeforeEach`, как в соседних spec-файлах)

## Context Files

**Feature artifacts:**
- [0011-feat-group-customization.md](docs/features/0011-feat-group-customization/0011-feat-group-customization.md) — user-spec: «Критерии приёмки», «Граничные случаи», «Риски» (почему перезагрузка обесценивает проверку перетаскивания), «Как проверить» → «Агент проверяет», шаги 0а и 5–10
- [0011-feat-group-customization-tech-spec.md](docs/features/0011-feat-group-customization/0011-feat-group-customization-tech-spec.md) — tech-spec: Testing Strategy / E2E tests (список сценариев), Decision 1, Decision 2, Decision 3, Decision 12, таблица Risks
- [0011-feat-group-customization-decisions.md](docs/features/0011-feat-group-customization/0011-feat-group-customization-decisions.md) — decisions log: что реально сделали задачи 6–9, включая возможные отклонения от плана
- [0011-feat-group-customization-code-research.md](docs/features/0011-feat-group-customization/0011-feat-group-customization-code-research.md) — исследование кода: устройство тестового харнесса, существующие E2E, SortableJS

**Project knowledge:**
> В проекте нет каталога `.claude/skills/project-knowledge/`. Роль базы знаний выполняют:
- [CLAUDE.md](CLAUDE.md) — конвенции проекта; разделы «Drag & Drop: SortableJS + Svelte Synchronization» и «Building & Testing»
- [docs/overview.md](docs/overview.md) — продуктовый контекст: группы и их назначение
- [docs/technical.md](docs/technical.md) — технические решения, модель данных

**Code files:**
- [tests/e2e/0011-group-customization.spec.ts](tests/e2e/0011-group-customization.spec.ts) — создать: весь набор сценариев фичи
- [tests/e2e/helpers.ts](tests/e2e/helpers.ts) — только читать: общие хелперы **в редакции Task 9**
- [tests/e2e/0007-dynamic-layout.spec.ts](tests/e2e/0007-dynamic-layout.spec.ts) — только читать: работа с попапом настроек доски, проверки раскладки, симуляция сохранности через `getDataStore` / `localStorage`
- [tests/e2e/0006-group-visibility.spec.ts](tests/e2e/0006-group-visibility.spec.ts) — только читать: скрытие/показ групп, «Отмена», клик по оверлею, переключение досок
- [tests/harness/main.ts](tests/harness/main.ts) — только читать: что умеет `window.__test`, как работает `resetData`, почему старт не читает `localStorage`
- [tests/harness/obsidian-mock.ts](tests/harness/obsidian-mock.ts) — только читать: `saveData` пишет в `localStorage['tm-test-data']`
- [src/ui/useSortable.ts](src/ui/useSortable.ts) — только читать: опции SortableJS, `draggable: '[data-task-id]'`, стратегия «отменить DOM → обновить стор»
- [src/ui/BoardLayout.svelte](src/ui/BoardLayout.svelte) — только читать: обёртки групп, `data-group-container`, `.tm-board-layout__notes`
- [src/ui/BoardSettingsPopup.svelte](src/ui/BoardSettingsPopup.svelte) — только читать: строки `data-settings-group`, поле названия, стрелки, прокручиваемое тело попапа
- [src/ui/GroupHeader.svelte](src/ui/GroupHeader.svelte) — только читать: `.tm-group-header__title`
- [src/ui/CollapsibleGroup.svelte](src/ui/CollapsibleGroup.svelte) — только читать: `.tm-collapsible-group__title`, `.tm-collapsible-group__body` внутри `{#if !collapsed}`
- [src/ui/EmptyState.svelte](src/ui/EmptyState.svelte) — только читать: `.tm-empty-state`, выбор подсказки после Task 6
- [src/i18n/en.ts](src/i18n/en.ts) — только читать: точные тексты `emptyState.*`, дефолтные названия групп, нейтральный ключ от Task 2
- [playwright.config.ts](playwright.config.ts) — только читать: `workers: 1`, `fullyParallel: false`, webServer

## Verification Steps

- Шаг 1: `npm run build` — продукт собирается (харнесс собирается отдельно, но сломанная
  сборка означает, что предыдущая волна не доехала)
- Шаг 2: `npx playwright test tests/e2e/0011-group-customization.spec.ts` — все сценарии
  зелёные; в выводе видны все четырнадцать
- Шаг 3: негативный контроль по списку из TDD Anchor — каждое перечисленное временное
  изменение даёт ожидаемый результат (падение там, где указано падение; зелёный там, где
  указан зелёный). Отдельно убедиться, что Сц.5 краснеет при удалении вызова перетаскивания
  и при сбросе на третью группу. Зафиксировать наблюдения в отчёте
- Шаг 4: `git diff` — временные поломки из шага 3 отсутствуют в финальном коде
- Шаг 5: `npx playwright test` — весь набор E2E, включая `core`, `0006`, `0007`, `0008`,
  проходит целиком
- Шаг 6: `grep -n "nth(\|moveTask\|goto(" tests/e2e/0011-group-customization.spec.ts` —
  убедиться, что `moveTask` не встречается в Сц.5, `goto` встречается только в `beforeEach`,
  а `nth(` (если есть) не применяется к элементам доски для проверки порядка

## Details

**Files:**
- `tests/e2e/0011-group-customization.spec.ts` — **новый файл**, единственный изменяемый.
  Структура по образцу соседей: импорты из `@playwright/test` и `./helpers`, `test.beforeEach`
  с `page.goto('/')` + `standardBeforeEach(page)`, затем локальные хелперы, затем сценарии,
  разбитые комментарными разделителями (Названия / Порядок / Перетаскивание / Edge cases) —
  ровно так оформлены `0006` и `0007`.

**Что уже есть в проекте на момент старта:**
- `tests/e2e/helpers.ts` — `standardBeforeEach`, `resetData`, `createTask`, `fillTaskForm`,
  `moveTask` (программный, мимо SortableJS), `expandGroup`, `waitForToast`,
  `openBoardSettings`, `saveBoardSettings`, `closeBoardSettings`, `openGroupSettings`,
  `saveGroupSettings`. Task 9 переводит их с поиска по названиям групп на идентификаторы —
  читай файл в актуальном виде, сигнатуры могли измениться.
- `window.__test` — `resetData(partial?)`, `getDataStore()`, `moveTask(...)`,
  `updateSettings(...)`. Больше ничего; новых методов харнесса эта задача не добавляет.
- Сохранение: `dataStore.persist()` → `plugin.saveData()` → `localStorage['tm-test-data']`.
  `persist()` асинхронный, поэтому снимок читать не сразу после клика, а через ожидание
  (`expect.poll` по содержимому `localStorage` либо после `saveBoardSettings`, который уже
  ждёт исчезновения попапа).
- Создание доски — `.tm-board-header__icon[title="Create board"]`, переключение —
  `page.selectOption('.tm-board-header__select', boardId)` (см. `0007`, Сц.7–8).
- `resetData(снимок)` дополнительно **сбрасывает активную доску на первую**
  (`activeBoardId = data.boards[0].id`) и первым делом удаляет `tm-test-data` из
  `localStorage`. Отсюда два правила: снимок читать до вызова, а после восстановления в
  многодосочных сценариях явно выбирать нужную доску.
- Локаль в харнессе принудительно `en` (`locale.set('en')` при старте и внутри `resetData`),
  поэтому дефолтные названия английские (`Backlog`, `Focus`, `In Progress`, `Org Intentions`,
  `Delegated`, `Completed`). Сверять дефолт можно с ними, но **находить** группы по ним нельзя.
  Переключить язык из теста нельзя: `updateSettings({ language })` пишет только в
  `dataStore.settings`, а стор `locale` харнесс выставляет напрямую и наружу этой возможности
  не отдаёт — метод смены локали в `window.__test` отсутствует, а `setLocale` вызывается только
  из точки входа плагина и экрана настроек, которые харнесс не
  вызывает и наружу не отдаёт.

**Dependencies:**
- Task 09 — существующие E2E и `helpers.ts` уже переведены на идентификаторы; без этого
  новые сценарии будут конфликтовать с общими хелперами.
- Task 07 даёт `data-group-container` на обёртке группы, Task 08 — `data-settings-group` на
  строке попапа, поле названия и стрелки. Точные имена классов у поля и стрелок смотри в
  `BoardSettingsPopup.svelte` после Task 8, а не выдумывай.
- Новых пакетов не требуется. `@playwright/test` и харнесс уже настроены; webServer
  поднимается автоматически из `playwright.config.ts`.

**Edge cases:**
- **Свёрнутая группа отсутствует в DOM.** У `backlog` и `completed` тело `[data-group-id]`
  лежит внутри `{#if !collapsed}` в `CollapsibleGroup.svelte` и появляется только в
  развёрнутом виде; вместе с ним отсутствует и `.tm-empty-state` этой группы. Обёртка
  `[data-group-container]` при этом есть всегда — на ней и держатся проверки порядка, на неё
  же всегда можно поставить DOM-метку. Для перетаскивания и для любых обращений к телу группу
  нужно сначала развернуть (`expandGroup`).
- **В группе «Выполнено» нельзя создать задачу.** `BoardLayout.svelte` передаёт
  `onAdd={null}` для `completed`, поэтому `createTask(page, 'completed', …)` там не сработает,
  а перенос задачи в эту группу дополнительно меняет статус. Везде, где нужна сворачиваемая
  группа с задачами (Сц.5 и любые проверки тела), бери `backlog`.
- **Последняя видимая группа.** Чекбокс видимости у единственной оставшейся видимой группы
  отключён (`0006`, Сц.9) — в Сц.4 и Сц.11 не доводи набор до одной видимой.
- **Отключённые стрелки.** У крайней группы стрелка в соответствующую сторону неактивна.
  Выбирай для перестановки группу из середины порядка, иначе клик по `disabled`-кнопке
  просто ничего не сделает, а тест покажет «порядок не изменился» и будет прав.
- **Скрытые строки в попапе остаются кликабельными.** Скрытая группа двигается стрелками по
  соседям списка — это ожидаемое поведение, не баг.
- **Пары половинной ширины перепаковываются.** После смены порядка `focus`/`inProgress` могут
  перестать быть парой, и класс обёртки сменится на `--half-alone`. Не завязывай проверки
  порядка на классы ширины — только на `order` и геометрию.
- **Геометрия и одна строка.** Две группы половинной ширины в одной строке имеют одинаковый
  `top`. Для них сравнивай `left`, а не `top`; для групп в разных строках — `top`. Надёжнее
  всего сравнивать ранг по `order`, а геометрию использовать как подтверждающую проверку.
- **`order` возвращается строкой.** `getComputedStyle(el).order` — строка (`"3"`); приводи к
  числу перед сравнением. Значение по умолчанию — `"0"`.
- **Одинаковые названия у разных групп допустимы** (user-spec). Не пиши утверждений,
  предполагающих уникальность названий.
- **Общий `localStorage` между тестами.** `standardBeforeEach` чистит данные и снимает
  зависшие оверлеи модалок; не забывай его в `beforeEach`, иначе набор станет
  порядко-зависимым.
- **Узкий viewport.** В Сц.12 не опускайся ниже 600px по ширине: медиазапрос переводит все
  группы в полную ширину (`0007`, Сц.21) и меняет раскладку. Уменьшай **высоту**, ширину
  оставь обычной. После `setViewportSize` дай кадру перерисоваться.
- **40 символов ровно.** Это верхняя граница ввода (`maxlength`). Строка должна быть заметно
  шире колонки заголовка, иначе усечения не будет и проверка станет бессмысленной — бери
  40 символов без пробелов.

**Implementation hints:**
- Ранг группы удобно считать одним `page.evaluate`: собрать все `[data-group-container]`,
  для каждого взять `dataset` и `getComputedStyle(el).order`, вернуть массив пар, а сортировку
  и сравнение делать уже в тесте. Один вызов на утверждение — меньше гонок, чем десяток
  точечных запросов.
- Настоящее перетаскивание в Chromium: навести на карточку (`locator.hover()`),
  `page.mouse.down()`, затем **несколько** `page.mouse.move(x, y, { steps: … })` — первый
  небольшой сдвиг, чтобы библиотека распознала жест, потом движение к центру тела
  группы-приёмника, потом ещё один `move` в ту же точку, и `page.mouse.up()`. Одного `move`
  почти всегда мало. Целевая точка — центр `[data-group-id="…"]` приёмника; если группа
  пустая, целься в её тело, а не в заголовок.
- Если карточка не «схватывается»: проверь, что тащишь за саму карточку, а не за кнопку
  удаления (`.tm-task-card__delete` стоит в `filter` SortableJS), и что тело группы-приёмника
  реально в DOM и видимо.
- Результат перетаскивания проверяй по данным, а не по DOM сразу после `mouse.up()`:
  SortableJS откатывает DOM и отдаёт ход Svelte (см. `useSortable.ts`), поэтому надёжнее
  дождаться `expect(...).toHaveCount(...)` на карточках в группе-приёмнике, а не читать DOM
  синхронно.
- Метку для проверки сохранности DOM-узлов ставь как свойство на самом элементе
  (`el.__tmProbe = 1`) через `page.evaluate` — атрибут Svelte может перезаписать при
  ре-рендере, свойство переживает только тот же самый узел. Обёртки
  (`[data-group-container]`) помечай все сразу, тела (`[data-group-id]`) — только те, что уже
  в DOM: `document.querySelectorAll('[data-group-id]')` вернёт лишь развёрнутые группы, и
  свёрнутый `backlog` в выборку молча не попадёт.
- Подсказку пустой группы ищи как `.tm-empty-state` **внутри** нужного
  `[data-group-container="…"]`: на пустой доске таких элементов шесть, глобальный локатор
  поймает не тот. Ожидаемые тексты бери из `src/i18n/en.ts` (`emptyState.*` плюс нейтральный
  ключ от Task 2), а не из головы — тексты правились в этой же фиче.
- Поле названия в строке попапа ищи внутри `[data-settings-group="…"]`. Пустоту проверяй
  через `toHaveValue('')`, дефолт — через `toHaveAttribute('placeholder', …)`. Не пиши
  `toHaveValue(дефолт)`: это утверждение проходит ровно тогда, когда контракт Decision 4
  сломан.
- Заголовок группы: обычные группы — `.tm-group-header__title`, сворачиваемые —
  `.tm-collapsible-group__title`. Ищи их внутри `[data-group-container="…"]`, тогда один
  хелпер закроет оба случая.
- Заголовок попапа настроек группы — тот же `.tm-popup__title`, что и у настроек доски;
  различай их по тому, каким способом попап открыт (`openGroupSettings` против
  `openBoardSettings`), а не по тексту.
- Прокручиваемый контейнер попапа в Сц.12 определи по факту (`scrollHeight > clientHeight`),
  а не по угаданному имени класса: точное имя задаёт Task 8.
- Не добавляй в `helpers.ts` ничего нового: Task 9 только что привёл этот файл в порядок, а
  твои хелперы специфичны для одной фичи. Держи их в spec-файле.
- Не расширяй `window.__test`: это чужой файл (`tests/harness/main.ts`) и чужая волна.
  Если чего-то принципиально не хватает — сначала доложи, а не правь харнесс молча.

## Reviewers

- **dev-test-reviewer** → `docs/features/0011-feat-group-customization/0011-feat-group-customization-task-10-dev-test-reviewer-review.json`
- **dev-code-reviewer** → `docs/features/0011-feat-group-customization/0011-feat-group-customization-task-10-dev-code-reviewer-review.json`
- **dev-security-auditor** → `docs/features/0011-feat-group-customization/0011-feat-group-customization-task-10-dev-security-auditor-review.json`

## Post-completion

- [ ] Записать краткий отчёт в [0011-feat-group-customization-decisions.md](docs/features/0011-feat-group-customization/0011-feat-group-customization-decisions.md) (Summary: 1-3 предложения, ревью со ссылками на JSON, без таблиц файндингов и дампов)
- [ ] Зафиксировать в отчёте результат негативного контроля Сц.5: покраснел ли сценарий без
      вызова перетаскивания и при сбросе на третью группу (обязательное доказательство
      падаемости), и что вариант с перезагрузкой действительно даёт ложно-зелёный результат
- [ ] Зафиксировать в отчёте оговорку из Сц.14: буквальная проверка перевода заголовков при
      смене языка из E2E недостижима (харнесс пинует `en`), проверено причинное условие —
      пустой `title` у всех шести групп
- [ ] Если какой-то сценарий из Testing Strategy не удалось покрыть — назвать его явно и
      объяснить, а не тихо опустить
- [ ] Если отклонились от спека — описать отклонение и причину
- [ ] Обновить user-spec/tech-spec если что-то изменилось
